# This module has been replaced by vector_store.py (SQLite + numpy).
# Kept as an empty stub to avoid stale .pyc import errors.
# Safe to delete once all caches are cleared.

raise ImportError(
    "milvus_store has been replaced by vector_store. "
    "Update your imports: from analyst.rag.vector_store import VectorStore"
)
