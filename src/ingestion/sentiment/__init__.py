"""Sentiment-lane ingestion (issue #76).

Houses the X (Twitter) ingestion clients + helpers. Mirrors the layout
of ``ingestion.news`` and ``ingestion.market`` — one folder per upstream,
public surface re-exported from ``__init__``.
"""
