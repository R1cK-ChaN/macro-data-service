# Architecture — current state

**Version:** 0.1.0 — *mini-Bloomberg foundations* (pre-1.0)
**Target:** 1.0 = mini-Bloomberg for macro + markets. One terminal-shaped
surface covering real-time quotes, macro time series, economic + corporate
calendar, research documents, news flow, release-aware scheduling, cross-source
resolution, and a RAG/agent layer — all off a single canonical store, pulled
from ~25 free/licensed upstreams instead of BBG's paid feed.
**Role in the monorepo:** the data-aggregation layer. Ingests, normalizes, and
resolves macro + market + calendar + document data from ~25 upstreams into a
single SQLite store that downstream services (analyst UI, RAG, agents) read
through one service interface.
**Last updated:** 2026-04-27 (branch `feat/issue-84-brazil-coverage`, Brazil coverage shipped — IBGE monthly release-calendar HTML connector + BCB Copom-history JSON scraper wired into the recurring scheduler and parity registry; the BCB JSON exposes target Selic + announcement date inline so the slice ships value-bearing rows for every Copom decision (RBA-style coverage), while IBGE remains schedule-only with per-release detail-page parsing deferred to P2)

### Bloomberg-capability coverage (current → 1.0)

| BBG capability | Our equivalent | Status |
|---|---|---|
| Real-time quotes, EOD bars | `market_price_bars` + Tiingo/EODHD/macro projection | shipped (issue #1) |
| `<GO>` command surface (HDB, ECO, CALT, …) | `macro-data-service` CLI + thin HTTP | shipped; expanding |
| ECO — economic calendar | `cal_econ_*` + official sources (BLS/BEA/Census/ISM/Fed/ECB/NBS/BoJ/Statistics Bureau JP/INSEE/ZEW/Ifo/GfK/HCOB); TE retained as historical source | shipped (issues #8, #9, #13 P2, #14 P1/P2, #15 P4a/P4b/P4c/P5, #23) |
| EVTS — corporate calendar | `cal_corp_*` + EODHD | in flight (issue #8) |
| Terminal News (N) | `news_articles` + 140-feed RSS pipeline + classifier | shipped core; dedup + ranking ongoing |
| Research / NIM | `document` + `document_blob` (+ RAG index) | shipped core; Fed + gov-report wired |
| PORT / PRTU analytics | `portfolio_*`, `trade_signals`, `performance_records` | scaffolded; awaiting downstream |
| FLDS / SRCH (metadata search) | `subjects` + `concept_map` + `list_items` family filter | shipped (issues #2, #5) |
| Release alerts, SRCY | `release_schedule` + `release_status` + 3 alert types | shipped |
| Terminal chat (IB / MSG) | `conversation_threads` / `_messages`, `group_*`, `delivery_queue` | scaffolded |

"Shipped" = tables + code in place. "In flight" = current work. "Scaffolded" =
schema exists; population pending downstream services.

This doc is the shared context surface for other agents (Claude subagents,
Codex, fresh sessions) starting cold. README.md is the user-facing intro; this
file is "what's wired up right now, where it lives, and what's in flight."

When the doc disagrees with the code, trust the code and update the doc.

---

## One-paragraph mental model

Sources → per-source ingestion clients → storage (SQLite, `engine.db`) → a
unified resolution layer (`resolve_indicator`, `get_market_history`,
`list_items`, soon `v_calendar_item`) → thin HTTP/CLI service. Every table is
keyed for point-in-time queries; revisions and vintages are preserved rather
than overwritten. Downstream never talks to upstream providers — it talks to
the resolution layer.

---

## Layers

| Layer | Lives in | Role |
|---|---|---|
| **Sources** | ~25 upstream APIs / RSS feeds / HTML endpoints | Raw data |
| **Ingestion** | `src/ingestion/` | Fetch, normalize, validate, write |
| **Storage** | `src/storage/{sqlite.py, schema.py, queries/}`, ~60 tables | Canonical persistence |
| **Resolution** | `src/macro_data/service/` + storage helpers | Cross-source ranking, PIT queries, unified views |
| **Service** | `src/macro_data/cli.py`, `server.py` | CLI + HTTP boundary |
| **RAG sidecar** | `src/rag/` | Local semantic index + retrieval (Milvus optional) |

Everything else (agents, UIs, notebooks) is a consumer.

---

## Domains and current status

### 1. Macro time-series (the backbone)

- **Sources wired** (13): BLS, FRED, EIA, NYFed, Treasury, IMF, Eurostat, Destatis, ZEW, BIS, ECB, OECD, World Bank. SDMX providers unified under `ingestion/sdmx/` (6 of the 13 go through it).
- **Schema:** `obs_source`, `obs_family`, `indicators`, `indicator_vintages`. `concept_map` bridges source-native IDs to 86 canonical concepts; `subject_aliases` lets text queries resolve back to concepts.
- **Resolution:** `resolve_indicator(concept_id)` ranks sources by `concept_map.priority`, returns primary + alternates. Vintages preserved for PIT reconstruction.
- **Status:** architecture complete; first-pass ingestion bootstrap underway. DB starts empty; Phase 1 (top US macro, 10 indicators) is the immediate next milestone, not more code.

### 2. Documents (government reports + central-bank comms)

- **5-table normalized schema:** `doc_source` → `doc_release_family` → `document` → `document_blob` + `document_extra`. Covers ~40 publishers across US/CN/JP/EU.
- **Clients:** `GovReportIngestionClient`, `FedIngestionClient` (Fed speeches/minutes), news pipeline for wire stories.
- **Linkage:** `obs_family_document` connects an obs family to the release family that produced it (produced_by / derived_from / related_to). Subject tagging happens at ingest via `storage/subjects.py`.

### 3. News

- **Clients:** `NewsIngestionClient` over 140 RSS feeds (`ingestion/news/_config.py`). Fingerprinting in `article_fingerprint` handles de-dup.
- **Classifier pipeline:** `news_classify.py` + `news_extract.py` for content extraction, domain detection, trend bucketing.

### 4. Market data (issue #1 — shipped)

- **Identity-first design.** Instruments resolved through OpenFIGI / ISIN / EODHD symbol history — `market_instruments` + `market_symbol_history` + `market_price_bars`.
- **Providers:** Tiingo (US equities/ETFs, P0), EODHD (global + delistings, P1), FRED/EIA/ECB projections (`MacroMarketProvider`, P2), `IdentityRepairService` (lazy repair on `break_detected`).
- **Universe:** 11 US macro ETFs + 6 global instruments seeded; extendable via `_tiingo_universe.py` / `_eodhd_universe.py`.

### 5. Calendar (issues #8 + #9 complete — official sources are the recurring ingestion path; TE retained as historical source)

Two physical lanes behind one downstream contract (see `src/storage/STORAGE.md` → "Unified calendar").

- **Economic lane** — `cal_econ_raw` / `cal_econ_event` / `cal_econ_drops` / `calendar_event_vintages`. **Active recurring ingestion:** BLS / BEA / Census / ISM / U Michigan / Conference Board / NAR / Federal-Reserve / Fed-Speeches / ECB / ECB-Speeches / EIA / DOL / ONS / BoE / BoE-Speeches / StatCan / BoC / ABS / RBA / MoSPI / RBI / KOSTAT / BOK / IBGE / BCB / Eurostat / Destatis / ZEW / Ifo / GfK / HCOB / INSEE / INE / ISTAT / NBS / BoJ / BoJ-Speeches / Statistics Bureau JP / CAO / MoF / METI — source-owned providers registered in `cal_provider` at `precedence=100`; schedule-side refresh runs daily, value-side sweep runs on a frequent cron. The four `*-Speeches` connectors (issue #56) ship schedule-only events anchoring central-bank speeches as calendar items (`actual=NULL`, no parity whitelist). **Historical source (ad-hoc only):** TradingEconomics (264,792 events 2013-01-02 → 2026-04-21; cutover 2026-04-22); `calendar_econ_backfill` / `calendar_econ_sync_updates` remain callable for gap-fill work but are not on any recurring schedule. `v_calendar_item` is a plain `UNION ALL` — both TE and source-owned rows are visible; callers filter by `provider=` to select a lane. `cal_provider.precedence` is used by the parity harness (`parity.py`) to classify matched/unmatched rows, not to suppress duplicates in the view.
- **Corporate lane** — `cal_corp_raw` / `cal_corp_event`. EODHD earnings / IPOs / splits / dividends / earnings_trends.
- **Unified view:** `v_calendar_item` `UNION ALL`s both lanes into the `CalendarItem` DTO shape. Downstream sees one target.
- **Revision model:** content-hash over mutable fields; append-only raw; PIT projection as the queryable layer. Mirrors `obs_family` / `indicator_vintages`. The dedicated `calendar_event_vintages` table (issue #21) records the per-event revision chain — `(event_id, provider, vintage_date, observed_at, actual, forecast, previous, metadata_json)` with `UNIQUE(event_id, provider, vintage_date)`. Connectors call `append_calendar_event_vintage_if_changed_with_conn(...)` before the `cal_econ_event` upsert; the vintage write runs unconditionally even when the `cal_econ_event` freshness gate suppresses an update, so out-of-order or LastUpdate-reused revisions still land. Read with `calendar_actual_as_of(event_id, provider, t)` — returns the vintage with the greatest `observed_at <= t` (julianday-normalized so fractional-second timestamps don't leak future state). `calendar_vintage_history(...)` returns the full chain.
- **Legacy `calendar_events`** compatibility table remains in schema for old DBs, but the HTML-scraped refresh path is retired. Legacy read helpers now read `cal_econ_event`, and public clients use `GET /v1/calendar` over `v_calendar_item`.

Slice progress:

- **P0 (shipped):** six new tables + `v_calendar_item` VIEW + extended `CalendarItem` DTO + 9 smoke tests.
- **P1 (shipped — scaffold):** TE API scaffold under `src/ingestion/calendar/te_api/` — `client.py` (auth / 1 r/s / 409 backoff / URL guard), `parser.py` (22-field → raw + event records, content-hash over 6 mutable fields), `projector.py` (idempotent raw inserts + snapshot-aware event upsert + drop audit), `backfill.py` (era-bracketed adaptive window planner + cursor-persisting runner), `updates.py` (`/calendar/updates` → `/calendarid` reconciler). Service ops `calendar_econ_backfill` + `calendar_econ_sync_updates` exposed over HTTP with `dry_run=True` default. Nothing auto-runs. 22 mocked `respx` tests — no real TE calls in CI.
- **P2 (shipped — scaffold):** EODHD corporate scaffold under `src/ingestion/calendar/eodhd_api/` — `client.py` (auth / 429 backoff / `fmt=json` injection), `parser.py` (five subtype parsers: `earnings` / `earnings_trend` / `ipo` / `split` / `dividend`; `provider_event_id = sha256(provider|subtype|code|primary_date|subtype_key)`; IPO id anchors on lifecycle-stable `filing_date`; per-subtype `content_hash` over mutable fields), `projector.py` (idempotent `cal_corp_raw` inserts + `observed_at`-gated `cal_corp_event` upsert), `fetcher.py` (subtype-dispatched window fetcher, trend-payload flattening for `[[…]]` shape). Service op `calendar_corp_fetch(subtype, from, to, symbols, dry_run)` — `dry_run=True` default.
- **P3 (shipped):** TE live-validation harness `scripts/validate_calendar_acquisition.py` (`--provider te`, dry-run default, budget-capped) + acquisition fixes from first live run — `_country_code` no longer synthesises false ISO codes (e.g. "IMF" → "IM"), and `/calendar/updates` 1000-row truncation is surfaced via `updates_truncated` on the reconciler summary.
- **P4 (shipped):** EODHD live-validation harness extension (`--provider eodhd`, 8 probes) + acquisition fixes — `/calendar/dividends` redefined as discovery-only `(symbol, ex_date)` (the extended dividend format does not arrive on the calendar feed); `_SubtypeSpec` grew `symbols_param` + `one_symbol_per_request` so dividends route through `filter[symbol]=X` (one request per symbol) rather than the generic `symbols=A,B` that the server silently drops.
- **P5 (shipped — scaffold):** per-ticker dividend enrichment via EODHD's `/api/div/{TICKER}.{EXCHANGE}`. New `parse_dividend_detail_row` shares `provider_event_id` with the discovery parser so the rich snapshot (amount / currency / declaration / record / payment dates) upserts the same `cal_corp_event` row. New `fetch_dividend_details` free function — one request per symbol, top-level-array payload (no envelope). Service op `calendar_corp_fetch_dividend_details(symbols, from, to, dry_run, max_requests)` — `dry_run=True` default. Validator gained a 9th probe (`dividend_details_aapl`) covering the enrichment feed.
- **P5a (shipped):** `/calendar/dividends` JSON:API pagination — `_SubtypeSpec` gained `paginated: bool`; `CorpCalendarFetcher._execute_request` loops `page[offset]` += `page[limit]` (1000) while `links.next` is truthy, trusting the link as the authoritative terminator rather than row-count heuristics. `max_requests` budget halts mid-cursor and surfaces `stopped_reason=max_requests_reached` so callers can retry. Closes the silent 1000-row truncation class of bug (same shape as the TE `/calendar/updates` issue surfaced in P3).
- **P6 (shipped — TE P1 backfill + country_code coverage):** First real TE backfill executed end-to-end (121 windows, 77,771 events, 2023-01-01 → 2026-04-21, zero truncation, zero drops, ~2:39 wall-clock, 121/1000 monthly quota). Live fidelity spot-check rehydrated 5 sampled `CalendarId`s via `/calendar/calendarid/` and matched 30/30 mutable-field comparisons. Surfaced a downstream-query gap: `TE_COUNTRY_MAP` covered 49 of 172 upstream `Country` values, leaving 16,837 rows (21%) with empty `country_code`. Map extended to 115 sovereign entries.
- **P6a (shipped — supra-national aggregate codes):** Five non-sovereign tags (`IMF`/`OPEC`/`World`/`G20`/`G7`) now map into ISO-3166-1's user-assigned `QM-QZ` range (`QM`/`QP`/`QW`/`QT`/`QS`) so the typed column stays strictly alpha-2 while still being filterable. Downstream filters via the new `SUPRA_NATIONAL_CODES` constant — `LIKE 'Q%'` is *wrong* because Qatar (`QA`) is also Q-prefixed. Full coverage: 77,771/77,771 rows (100%) mapped, zero empty `country_code` in the P1 backfill.
- **P7 (shipped — in-session truncation re-fetch + bracket tightening + TE P2 backfill):** The first P2 run (2016-2022, 164 windows, 20d/12d brackets) surfaced a silent-data-loss bug: when a window hit the 1000-row cap, the runner saved the truncation cursor but then advanced to the next *planned* window, dropping the tail `[last_date, window.end]`. Because the final window always advanced past the phase end, the cursor never rewound on a subsequent invocation either — 255 days / 51 consecutive-day runs were permanently missing. Fix: runner now uses a `deque` and enqueues a continuation `Window(last_date, window.end)` at the head of the queue when truncation is detected; raw-table content-hash dedup absorbs the overlap at `last_date`. Progress guard prevents infinite loops on pathological 1000+ rows on a single date. Also tightened `ERA_BRACKETS` for 2016-2022 from 20d/12d to 10d (P1's proven-safe size), which eliminated truncation entirely on the clean re-run (256/256 windows, zero truncated). P2 events post-fix: 152,884 (was 136,531). Missing-day count: **40** (was 255) — all weekend or sparse-holiday upstream, verified. New `continuation_fetches` field on `RunSummary` makes the dedup pass visible to callers.
- **P7a (shipped — discontinued-coverage country map extension):** P2's 2016-2022 run surfaced 1,773 events across 40 small-state countries (Afghanistan, Andorra, Syria, Puerto Rico, Greenland, Isle of Man, San Marino, Monaco, Liechtenstein, various Pacific/Caribbean nations, etc.) that TE published through 2019-2022 but has since dropped from the feed — they never appeared in P1's 2023-era enumeration so the original map missed them. Map extended to 208 entries total. Post-projection coverage on the combined P1+P2 dataset: **230,655 / 230,655 rows (100%)** mapped, zero empty `country_code`. Historical events from TE's wider coverage era stay queryable even after upstream narrowed.
- **P8 (shipped — TE P3 backfill, full 2013→today coverage complete):** 2013-01-02 → 2015-12-31, 22 planned windows on 180d/30d brackets, 46 total fetches (22 planned + 24 in-session continuations — the P7 fix earning its keep on the sparse-era's oversized windows), 34,137 events, ~3 min wall-clock. 146 upstream-sparse days in the P3 range (TE's calendar coverage ramped up through Feb–Mar 2013: Jan-2013=36 events, Feb=264, Mar=996, plateau ≥900/mo from Apr onward; 2015-12=1,166). Verified upstream-empty via direct single-day probes. Fidelity spot-check on 3 random CalendarIds across Mar-2013/Apr-2014/Oct-2015 matched 18/18 mutable-field comparisons against `/calendar/calendarid/` rehydration. **Combined store post-P8: 264,792 events, 2013-01-02 → 2026-04-21, 100% country-code coverage across P1+P2+P3.** Monthly TE budget used this session ≈ 600 / 1000.

Issue #9 — official-source replacement (in progress on `feat/issue-9-official-calendar-connectors`):

- **P0 (shipped — provider registration + shared scaffolding):** Five official-source providers (`bls` / `bea` / `federal-reserve` / `ecb` / `nbs`) seeded into `cal_provider` at `precedence=100` (above TE's `10`); BLS / BEA / NBS typed as `government_agency`, Fed / ECB typed as `central_bank`. New `src/ingestion/calendar/_official_shared/` package exposes three utilities every P1–P5 connector will reuse: `canonicalize_indicator` (alias table: `"Consumer Price Index" → "CPI"`, `"FOMC Rate Decision" → "FOMC_RATE"`, …), `parse_scheduled_release_time` (DST-aware — BLS `"8:30 AM ET"` resolves correctly across winter/summer), and `synthesize_event_id` (`sha256(provider|country|indicator_canonical|event_time_utc)` — upstream sources without stable ids get a deterministic key the projector can upsert on). `scripts/validate_calendar_acquisition.py` gained `--provider {bls,bea,fed,ecb,nbs}` flags (scaffold-only; probe bodies land per-phase). 10 smoke tests cover the shared utilities + a synthetic BLS row round-tripping through `v_calendar_item`.
- **P1 (shipped — BLS connector scaffold):** `src/ingestion/calendar/bls_api/` — calendar-side projection layer on top of the existing `ingestion.timeseries.scrapers.bls.BLSClient` (reused verbatim for auth / batching / 500-req-daily budget tracking). Parser turns a `BLSObservation` into `(raw, event)` records using the `_official_shared` utilities (`canonicalize_indicator` → `"CPI"`, `synthesize_event_id` for deterministic ids anchored on the reference-period date — stable across the value → value+schedule lifecycle). Projector mirrors the TE SQL shape (store_raw INSERT-OR-IGNORE + project_events upsert with observed-at ordering). Whitelist ships with two anchors — CPI (`CUUR0000SA0`) and NFP (`CES0000000001`) — the highest-trader-impact US releases; additional indicators (Core CPI / PPI / JOLTS / ECI / Productivity / Jobless Claims) land as later slices. Service op `calendar_econ_fetch_bls(start_year, end_year, series_ids, dry_run=True)` exposed on `LocalMacroDataService`; `dry_run=True` default. `event_time_utc` on P1-only rows is the **reference-month end** with `event_time_precision='approximate'` — a deliberate placeholder that P1a's schedule scraper overwrites with the true scheduled datetime. 18 mocked tests — no real BLS calls in CI. No live probe this slice (user elected 1a scope).
- **P1b (shipped — BLS live-probe scaffold):** `scripts/validate_calendar_acquisition.py` gains a working `--provider bls` flow. Two probes (CPI / NFP over a two-year window) drive the production `BLSClient.get_series_single` path, diff returned observation rows against the field set the calendar parser reads (`year` / `period` / `periodName` / `value` / `footnotes` / `latest`), flag period-code / value-type anomalies, and dry-parse each observation through `parse_observation` to confirm the calendar projection still maps cleanly. Dry-run default mirrors the TE / EODHD ergonomics; `--execute` hits real BLS and writes a markdown report under `docs/validation/calendar_acquisition_bls_<date>.md`. Still user-driven — no recurring fetch is scheduled by this slice. 16 mocked tests (planner + diff + dry-parse + runner auth-missing / http-error / empty / happy-path). BEA / Fed / ECB / NBS still print the scaffold-registered stub until their own live probes land.
- **P1a (shipped — release-schedule scraper):** `src/ingestion/calendar/bls_api/schedule.py` scrapes `bls.gov/schedule/news_release/<series>.htm` (CPI + NFP), parses the `<table class="release-list">` DOM with BeautifulSoup, and projects each upcoming-release row into `cal_econ_event` with `actual=NULL` / `event_time_precision='datetime'`. Release times (`"08:30 AM"`) are DST-aware converted to UTC via `_official_shared.parse_scheduled_release_time` with `default_tz="America/New_York"` (BLS publishes all major indicators at 8:30 AM ET, unadorned on the page). Live fetch uses a browser-style header bundle because `bls.gov` returns HTTP 403 on the default `python-requests` UA. New `project_schedule_events` upsert splits the write paths: schedule side owns `event_time_utc` / `precision` / `title` / `source_url`; API side owns `actual` / `previous` / `content_hash`. `project_events` cross-source merge rule preserves an already-datetime-precise `event_time_utc` when a later API-side write would otherwise clobber it with the approximate placeholder. Result: schedule + API rows converge on one `cal_econ_event` row regardless of write ordering, via the shared `provider_event_id` (anchored on reference-period date in both sides). Service op `calendar_econ_schedule_bls(series_ids, dry_run=True)`. Fixture HTML captured live 2026-04-21 in `tests/fixtures/bls_schedule/`; 12 new tests cover parse / merge (both orderings) / DST / service wiring. No live probe in CI.
- **P2 (shipped — BEA connector scaffold):** `src/ingestion/calendar/bea_api/` — calendar-side projection on top of the existing `ingestion.timeseries.scrapers.bea.BEAClient` (reused verbatim for auth / ~100-req-per-min throttle / error handling). Parser turns a `BEAObservation` into `(raw, event)` records using the same `_official_shared` utilities (`canonicalize_indicator` collapses the BEA release title to a canonical token; `synthesize_event_id` anchors the id on the BEA-canonical reference-period date — stable across the value → value+schedule lifecycle that P2a will introduce). Whitelist ships with two anchors — **Real GDP** (NIPA `T10101` line 1, quarterly SAAR — the advance / second / third estimate headline) and **Personal Income** (NIPA `T20600` line 1, monthly — the headline aggregate on BEA's Personal Income and Outlays release). PCE lives on the same monthly release but at a different `(table, line)` coordinate; rather than ship a guess at the coordinate with no live probe to verify, PCE is deferred to P2a. Trade Balance (ITA dataset) and Corporate Profits are deferred to P2b / P2c because the ITA parameter surface differs from NIPA and warrants its own live probe. Fetcher groups whitelist entries by `(dataset, table, frequency)` so one BEA HTTP call satisfies every line on a shared table; off-whitelist lines in the response are discarded rather than projected as unknown-indicator rows. `BEAObservation` gained a `raw: dict` field (default empty) so `NoteRef`-only revisions register new audit rows — same pattern applied to `BLSObservation` in P1. Projector mirrors the BLS SQL shape including the `datetime`-precision preservation clause; BLS + BEA are now the two concrete callers of the merge-rule variant, so promotion into `_official_shared` waits for a third caller (likely ECB / Fed / NBS). Service op `calendar_econ_fetch_bea(start_year, end_year, series_ids, dry_run=True)`. `event_time_utc` on P2-only rows is the **reference-period end** with `event_time_precision='approximate'` — the P2a schedule scraper (bea.gov/news/schedule) will overwrite with the true scheduled datetime. 22 mocked tests cover registry / parser / projector / fetcher / service op; no real BEA calls in CI.
- **P3 (shipped — ECB connector scaffold):** `src/ingestion/calendar/ecb_api/` — calendar-side projection on top of the existing `ingestion.timeseries.sdmx.providers.ecb.ECBClient` (points at `data-api.ecb.europa.eu/service`; no auth). Parser turns an `SDMXObservation` into `(raw, event)` records using `_official_shared` utilities. Whitelist ships with the three ECB key policy rates from the `FM` dataflow — `ECB_MRO` (`FM.B.U2.EUR.4F.KR.MRR_FR.LEV`), `ECB_DFR` (`FM.B.U2.EUR.4F.KR.DFR.LEV`), `ECB_MLF` (`FM.B.U2.EUR.4F.KR.MLFR.LEV`). All three ship together because the Governing Council moves them simultaneously at each policy meeting; the projector gives each a distinct `provider_event_id` via canonical indicator differentiation so they don't collapse on shared observation dates. The three `ECB_*` canonical tokens were added to `_official_shared/canonicalize.py` so the round-trip (`canonicalize_indicator("ECB_MRO") → "ECB_MRO"`) matches TE's `"MRO rate"` / `"Main Refinancing Operations Rate"` → `"ECB_MRO"`, a prerequisite for P6 parity. Economic Bulletin + monetary-policy press conference timing are calendar-only (no value-bearing feed); they land in the P3a slice that will scrape `ecb.europa.eu/press/calendars/`. ECB's SDMX observations carry the rate's **effective date**, not the decision date — calendar rows therefore ship with `event_time_precision='approximate'` at 00:00 UTC on the effective date, pending P3a's meeting-calendar scraper that will upgrade the placeholder to the announcement datetime (CET-aware) on the shared `provider_event_id`. BLS + BEA + ECB are now three concrete callers of the merge-rule projector variant — the promotion threshold for `_official_shared` is met, but P3's minimum slice is the ECB scaffold itself; projector consolidation is a follow-up subtraction commit. Service op `calendar_econ_fetch_ecb(start_period, end_period, series_ids, dry_run=True)`. 21 mocked tests cover registry / parser / projector / fetcher / service op; no real ECB calls in CI.
- **P4 (shipped — Fed connector scaffold):** `src/ingestion/calendar/fed_api/` — HTML scraper against `federalreserve.gov/monetarypolicy/fomccalendars.htm` (no Fed calendar API exists). Whitelist ships with a single anchor — `FOMC_RATE` (FOMC rate decision). The scraper walks each `<div class="panel panel-default">`, extracts the year from the `<h4>YYYY FOMC Meetings</h4>` heading, and iterates `<div class="row fomc-meeting">` rows for month + date. The closing day of a range (``"27-28"`` → day 28) is the rate-decision day; cross-month pairs (``"Jan/Feb"`` + ``"31-1"``) resolve the closing day to the second month, with a Dec/Jan pair bumping year by one — historical meetings like Jan 31-Feb 1 2023 and Oct 31-Nov 1 2023 depend on this. Trailing asterisks on the date cell (``"17-18*"``) flag meetings that publish the Summary of Economic Projections; the flag rides on the event record's title. `event_time_utc` is computed at **14:00 ET on the closing day** (DST-correct via `_official_shared.parse_scheduled_release_time`) — the Fed's standing announcement convention since 2013. `provider_event_id` anchors on the closing-date ISO string so the id is stable across the schedule → value upgrade (P4b-values scrapes the FOMC statement page for the target-range number and upserts on the same id). Live fetch uses a browser-UA header bundle (`federalreserve.gov` also 403s on default `python-requests`) and advertises only `gzip, deflate` (Brotli decoding requires a package not in the declared dependency set). Projector mirrors the BLS / BEA / ECB merge-rule SQL; Fed is the fourth concrete caller — projector consolidation into `_official_shared` still deferred (separate subtraction commit). Beige Book, SEP, H.4.1, H.8, and scheduled Fed speeches land in P4a (separate `releasedates.htm` scrape with a different DOM). Service op `calendar_econ_fetch_fed(dry_run=True)` exposed on `LocalMacroDataService`. Fixture HTML captured live 2026-04-21 in `tests/fixtures/fed_fomc_calendar/` (2026 current-year + 2027 future-year panels); 24 mocked tests cover parse / cross-month / DST / projector / fetcher / service op — no real Fed HTTP in CI.
- **P5 (shipped — NBS connector scaffold):** `src/ingestion/calendar/nbs_api/` — HTML scraper against NBS yearly-calendar articles (`stats.gov.cn/english/PressRelease/ReleaseCalendar/<YYYYMM>/t*.html`). No NBS calendar API exists. Whitelist ships with a single anchor — `CPI` (China Consumer Price Index), 12 scheduled monthly releases per year at 09:30 Asia/Shanghai. The scraper finds the main release-schedule table (`<table class="trs_word_table">`), iterates indicator rows pairwise with the time-row that follows each (table shape: 14 cells per indicator row = No + Content + 12 months; each month cell is `"9/Fri"` or `"……"` for no-release), matches content labels against the indicator whitelist by substring, and skips empty-month markers. Year is read from the `<title>` (`Regular Press Release Calendar of NBS in 2026`); callers may override. `event_time_utc` combines the release date with the per-row release time via `_official_shared.parse_scheduled_release_time(default_tz="Asia/Shanghai")`. `provider_event_id` anchors on the ISO release-date + canonical indicator so the id is stable across the schedule → value upgrade a future P5a slice might add (value-side scrape of each press-release article). Projector is the fifth concrete caller of the merge-rule SQL — the `_official_shared` consolidation subtraction pass is now squarely overdue and lands next. Service op `calendar_econ_fetch_nbs(calendar_url, year?, dry_run=True)` requires an explicit calendar-article URL in execute mode (no auto-discovery yet — the release-calendar index at `NBS_CALENDAR_INDEX_URL` links to one article per year; autodiscovery is P5a). Fetcher raises `NBSCalendarParseError` on a zero-entry parse in execute mode (the NBS is the issue's flagged highest-risk upstream — HTTP-only `http://` by default, HTML-fragile, frequent non-CN timeouts). Fixture HTML captured live 2026-04-21 in `tests/fixtures/nbs_calendar/`; 25 mocked tests — no real NBS HTTP in CI. PPI, GDP, Industrial Production, Fixed Asset Investment, Retail Sales, Manufacturing PMI, Non-manufacturing PMI extend the same whitelist when the scraper shape is validated against a live probe.
- **P7 (shipped — `GET /v1/calendar` HTTP route):** Downstream consumers can now query the unified calendar via HTTP without reaching into `engine.db` or importing from `src/`. `SQLiteEngineStore.list_calendar_items(...)` reads `v_calendar_item` with equality filters on `domain` / `country` / `ticker` / `subtype` / `provider` and offset pagination; result rows are shaped to the `CalendarItem` DTO. Service op `list_calendar_items` wraps the store call in a JSON:API envelope (`{data, meta: {count, offset, limit}, links: {next}}`) — `links.next` is an opaque cursor object (`{offset, limit}`) the client sends back, keeping the server free to migrate to cursor-based pagination later without a breaking change. HTTP route `GET /v1/calendar` on `macro-data-api` parses query params (`page[offset]` / `page[limit]` mapped into the op's `page_offset` / `page_limit` arguments) and forwards to the service. Same auth model as other reads: no token required for GET, still unauthenticated. 18 new tests cover storage filters + pagination, service-op envelope shape, and the HTTP surface end-to-end (real `ThreadingHTTPServer` on a free port driven by `http.client`). Shipped ahead of P6 because P7 has no upstream dependency — runs over the already-populated 264,792-row TE + EODHD table — while P6 needs real official-source rows to compare against.
- **P5b (shipped — `_official_shared` projector consolidation):** Subtraction pass. `store_raw` / `project_events` / `project_schedule_events` lifted into `src/ingestion/calendar/_official_shared/projector.py`; BLS / BEA / ECB / Fed / NBS projectors became thin re-exports. Shared SQL adopts the corrected merge CASE first landed in NBS P5: an incoming `datetime`-precision row overwrites a stored `datetime` row (schedule revisions land rather than getting silently swallowed), and the preservation path fires only when the incoming precision is less granular (API-side `approximate` lands on top of a schedule-side `datetime`). Before the consolidation BLS/BEA/ECB/Fed all shipped the older "stored datetime always wins" shape — a latent correctness bug that this pass removes across all five connectors. New `tests/test_official_shared_projector.py` parametrises over every connector's `*CalendarEventRecord` dataclass to lock the invariant in (17 regression specs). No external surface change — every downstream import goes through the thin per-connector re-export modules.
- **P4b-values (shipped — FOMC statement value-side scrape):** `src/ingestion/calendar/fed_api/statements.py` scrapes the statement page for each FOMC meeting (`federalreserve.gov/newsevents/pressreleases/monetary<YYYYMMDD>a.htm` — `a` is the statement, `b` and higher are the implementation note and related supplementary releases) and fills the `actual` column on the existing schedule row written by P4. Parser anchors on "decided to (maintain\|keep\|raise\|lower) the target range for the federal funds rate [by N percentage point(s)] (at\|to) X to Y percent" (the `keep` verb is the Fed's preferred hold wording in the 2020-2022 zero-bound era — without it, auto-discovered pre-2022 rows would land as parse failures); range endpoints support the Fed's mixed-number notation (`"4-1/4"` = 4.25), bare fractions for the zero lower bound (`"0 to 1/4"` = 0.00-0.25), and the non-breaking hyphen (`&#8209;`) the page routinely uses to keep mixed numbers from wrapping. Parser also lifts the page's own `"For release at X:XX p.m. E(S\|D)T"` clock into `StatementValue.release_time_local`, so emergency off-schedule statements (the 2020-03-15 inter-meeting cut went out at 5:00 p.m. EDT) land at the published time rather than the standing 14:00 ET default. The formatted `actual` is a two-decimal range string (`"4.25-4.50"`); decision direction, the optional "by N percentage point(s)" phrase, and the release clock persist in the raw payload for revision diffs. Top-level `fetch_fed_statement_values(connection, closing_dates=?, dry_run=True, html_fetcher=?)` auto-discovers past FOMC rows with `actual IS NULL` from `cal_econ_event` when `closing_dates` is omitted and reads `has_sep` from the stored row's title (`"FOMC Rate Decision + SEP"` suffix) so the value-side `project_events` upsert doesn't silently drop the SEP marker from quarterly projection-materials meetings; per-URL fetch / parse failures land in the summary (`fetch_failures` / `parse_failures`) rather than aborting the run so a single 404 during an upstream layout change can't block progress on the other pages. Service op `calendar_econ_fetch_fed_values` exposes the driver. **Projector split lands here:** `fetch_fed_calendar` (schedule-side write) flips from `project_events` to `project_schedule_events` so a later schedule re-scrape can't clobber the published `actual` — this was the Codex P4 rule-3 deferral tied explicitly to P4b-values. `fetch_fed_releasedates` stays on `project_events` (no value lane on those indicators). `provider_event_id` on the value side matches the schedule side exactly (shared `synthesize_event_id(..., closing_date.iso)` anchor), so the `actual` upserts onto the existing row via the shared projector's merge CASE — datetime precision survives. Five trimmed statement fixtures in `tests/fixtures/fed_fomc_statements/` cover hold / 25bp hike / 50bp cut / zero-lower-bound emergency-cut / pre-2022 `keep`-verb hold; 24 mocked tests, no real Fed HTTP in CI.
- **P-sched-1 / P-sched-2 / P-sched-3 (shipped — recurring refresh + value-side sweep + circuit breakers):** `src/ingestion/calendar/scheduler.py` exposes two daily/frequent-cron entry points sharing the same per-connector loop. `refresh_all_schedules(connection_factory, dry_run=True, connectors=?)` is the schedule-side half (daily cron): iterates every connector's forward-looking schedule scrape (`schedule_bls_calendar` / `schedule_bea_calendar` / `schedule_census_calendar` / `schedule_ism_calendar` / `schedule_umich_calendar` / `schedule_conference_board_calendar` / `schedule_nar_calendar` / `schedule_ecb_calendar` / `schedule_eurostat_calendar` / `schedule_destatis_calendar` / `schedule_zew_calendar` / `schedule_insee_calendar` / `schedule_ine_calendar` / `schedule_istat_calendar` / `fetch_fed_calendar` / `fetch_fed_releasedates` / `fetch_nbs_calendar`). `sweep_value_side(connection_factory, dry_run=True, start_year=?, end_year=?, start_period=?, end_period=?, connectors=?)` is the value-side half (frequent cron — delivers "within minutes of publication"): iterates the value-bearing connectors (`bls` / `bea` / `census` / `ism` / `umich` / `conference-board` / `nar` / `ecb` / `eurostat` / `destatis` / `zew` / `insee` / `ine` / `istat` / `fed-values`), building per-connector closures that capture the resolved year/period config and construct the appropriate client lazily. NBS has no value-side op yet and is absent from the value-side plan. Each connector gets its own connection / commit / rollback lifecycle so an ECB 502 or NBS timeout rolls back only that connector's partial writes while the others stay committed; per-connector exceptions land on the returned `RefreshRunSummary.results` as `ConnectorResult(ok=False, error=...)` rather than aborting the pass. Subset argument (`connectors=["bls", "fed-values"]`) lets an operator re-run only specific sources after triage. `_connector_overrides` is a test-only seam so unit tests can exercise the aggregation + isolation logic against fake functions. Driver also inspects the returned summary for in-summary failure markers — BEA's `fetch_error`, ISM's `fetch_error`, U Michigan's `fetch_error`, Conference Board's `fetch_error` / `series_failed`, NAR's `fetch_error` / `series_failed`, Fed-releasedates' `fetch_error`, ECB's `fetch_errors` dict, BLS's `series_failed` list, Destatis/ZEW/INSEE/INE/ISTAT `series_failed`, Fed-values' `fetch_failures` / `parse_failures` lists — and flips `ok=False` with the recorded reason so a 502 / layout drift / per-URL 404 caught by the connector (rather than raised) doesn't masquerade as a clean run. Unknown connector names in the `connectors=[...]` subset (``"fed-fomcc"`` / operator typos) land on `RefreshRunSummary.unknown_connectors` and count toward `failed_count`. **Circuit breaker (P-sched-3):** `src/ingestion/calendar/scheduler_state.py` persists per-connector consecutive-failure counts + cool-down windows in the new `calendar_connector_state` table. After three consecutive connector-wide outages (thrown exception, `fetch_error` singular, BLS `series_failed` covering every planned series with no `series_ok`, Fed-values `meetings_fetched == 0` with planned meetings, or ECB `fetch_errors` with zero `entries_parsed` / `events_upserted`), a connector enters a 15-minute cool-down — subsequent sweeps skip it with `ok=False` and a `"cooling until …"` reason rather than hammering a broken upstream. Partial failures (one BLS series out of nine, one FOMC statement URL out of eight) land as `ok=False` on the card but do **not** count toward the breaker — reachability was demonstrated and the working items landed, so blocking the next sweep would delay fresh values on the good subset. `_summary_is_total_outage` distinguishes full from partial: for multi-page connectors (ECB scrapes both the GC-meeting page and the Economic Bulletin page), a `fetch_errors` dict with one page down but `entries_parsed > 0` from the other page counts as partial — the working page delivered rows and shouldn't be blocked on its neighbor's outage. A successful or partial sweep resets the counter and clears the cool-down immediately. `cooling_until_ms` is anchored on the actual failure moment (re-computed at mark time), not the connector's start timestamp, so a long-running connector (Fed-values walking 30s statement timeouts for eight pages) still gets a full 15-minute cool-down rather than one already-partly-consumed. Breaker state is keyed by scheduler-level connector name so a BLS outage trips the breaker for both schedule-side and value-side sweeps. The driver uses a separate state connection so a connector's data-side rollback can't unwind the failure counter. Dry-run checks state (so the preview reflects what would be skipped) but never mutates it. Service ops `calendar_econ_refresh_schedules` + `calendar_econ_sweep_values` expose the two drivers with identical JSON envelopes; 41 tests, no real upstream HTTP in CI. The ECB and Eurostat value-side default window is ~180 days (six months in `"YYYY-MM"` SDMX form) to keep the frequent-cron path cheap; Destatis uses the resolved year bounds; ZEW, INSEE, INE, and ISTAT value sweeps auto-discover due scheduled rows from `cal_econ_event`. An explicit `start_period` overrides ECB / Eurostat one-off historical backfills. The service op distinguishes key-absent (full plan) from key-present-empty (`connectors=[]` → run nothing) so an operator filtering a list down to empty doesn't silently fall back to the full plan. Health-telemetry wiring into `get_source_health_dashboard` is a follow-up P-sched-4 slice.
- **P-sched-3-budget (shipped — per-connector daily request-count tracking):** `calendar_connector_state` grew two columns (`requests_today` / `requests_day_utc`) that persist a UTC-day request counter per connector, surviving across the short-lived cron-invocation processes that the in-memory `BLSClient._daily_query_count` cannot. `scheduler_state.DAILY_BUDGET_CAPS = {"bls": 490}` holds the only currently-declared cap (matches the BLS in-memory soft-cap below the 500/day hard limit); omitted connectors are uncapped and the check is a no-op. `is_budget_exhausted(state, cap, today_iso=…)` rolls over on UTC day change so a fresh day starts from zero budget. `record_connector_requests(conn, name, requests_made, today_iso=…)` accumulates inside a day and resets across day boundaries. BLS's calendar `FetchRunSummary` grew `requests_made` (derived from `client.daily_query_count` before/after `get_series`); the scheduler's `_run_connector_with_breaker` reads it off the summary and updates the persisted counter. **Cap scope: value-side only.** `budget_cap: int | None` is a parameter on `_run_connector_with_breaker` — `sweep_value_side` passes `DAILY_BUDGET_CAPS.get(name)`, `refresh_all_schedules` passes `None`. The schedule-side BLS path scrapes `bls.gov/schedule/…` HTML (uncapped) and must stay runnable even when the value-side cron exhausted the `api.bls.gov` quota; otherwise the forward calendar would freeze from noon UTC to the next UTC midnight every day the cap was hit. **Failure-path accounting.** The value-side `_bls_values` closure snapshots the client counter before invoking `fetch_bls_calendar` and, on exception, attaches `exc.requests_made` with the delta before re-raising; the driver's exception handler records that count into the persisted counter so a partial-then-fail run (chunk 1 = 200 OK, chunk 2 = 500) still ticks the budget. Breaker success/failure marks deliberately preserve budget columns (`mark_connector_success` / `mark_connector_failure` UPDATE clauses omit them) — exhaustion is a volume signal that a mid-day success shouldn't zero out. **UTC-midnight correctness.** `today_iso` is resolved at record time (not sweep start) via the shared `scheduler_state.today_utc_iso()` helper so a sweep that crosses midnight attributes its post-reset delta to the new day, aligning with the client's own midnight reset. 21 new tests, no real upstream HTTP in CI.
- **Issue #37 (shipped — schedule-aware burst polling):** `sweep_value_side` now consults the schedule-side `event_time_utc` already living in `cal_econ_event` to decide whether each value-side connector needs a burst on this run. At sweep start the driver resolves a wall-clock burst deadline `sweep_start + 30min` and, for each connector with a registered predicate in `_VALUE_SIDE_DUE_ROW_FILTERS`, counts unfilled rows whose schedule-side `event_time_utc` falls inside a per-connector half-open event window `[sweep_start − 1h − fetch_buffer, sweep_start + 30min − fetch_buffer)` — exclusive upper bound, because a row at exactly the +30-min boundary would otherwise be counted as due even though the burst's last attempt (~29 min after sweep start with default 30 attempts × 60s spacing) cannot reach it. The `_VALUE_SIDE_FETCH_BUFFER` map captures connector-internal "wait N hours past schedule" semantics (BoJ MPM, BoJ Tankan, MoF, Statistics Bureau, METI, CAO GDP all wait 1 h before their fetcher will accept a row); shifting the count window by the same buffer keeps the burst from spinning on rows the connector cannot yet fetch. Predicates also exclude schedule-only rows that share a provider with value-bearing rows: `fed-values` keeps only `'FOMC Rate Decision%'` titles (`fed-releases` writes Beige Book / H.4.1 / H.8 schedule rows that never receive `actual`); `boj` splits into MPM (`title = 'BoJ Interest Rate Decision'`) vs Tankan (`title LIKE 'Tankan %'`); `cao` splits into Consumer Confidence (`title = 'Consumer Confidence'`) vs SNA GDP (`title LIKE 'GDP %'`). Connectors with zero due rows run once — the existing hourly baseline behaviour. Connectors with at least one due row run in a burst loop on top of the existing breaker helper: up to `_BURST_MAX_ATTEMPTS=30` invocations spaced by `_BURST_INTERVAL_SECONDS=60`, exiting early when a fresh count shows zero remaining (`burst_stopped_reason="actual_filled"`), the breaker short-circuits the next attempt (`"breaker_or_budget"`), the wall clock advances past `burst_deadline_dt` (`"window_closed"`), or the attempt cap is reached (`"max_attempts"`). Burst attempts go through `_run_connector_with_breaker` unchanged, so per-connector cool-downs and the `requests_today` budget remain authoritative — a 5-NFP-per-day BLS burst worst-case (5 × 30 = 150 calls) sits well inside the 490/day cap. Dry-run skips the burst (no fetch can fill `actual` so the loop would only spin) and preserves the pre-#37 plan envelope; on execute, when burst ran the connector's `summary` dict gains `burst_attempts` / `burst_initial_due` / `burst_stopped_reason` keys for operator triage. Tests inject `_burst_clock` / `_burst_sleep` / `_burst_max_attempts` to drive the loop without real time. **Slice caps:** ECB is intentionally absent from the burst predicate map — its value fetcher writes new `'ECB % Rate'` rows rather than filling `actual` on the `'ECB Monetary Policy Decision'` / `'ECB Economic Bulletin'` schedule rows, so neither row shape supports the burst's completion check; ECB falls through to the hourly baseline (8 decisions/year, ~30-min average lag is acceptable). Bursts are also strictly sequential per connector — when two connectors have due rows in the same sweep, the earlier one's full retry budget runs before the later one gets its first attempt. Both are P2 follow-up candidates if/when measurement shows the gap matters. The hourly `calendar-value-sweep.timer` baseline still fires every `:15` as the catch-all — burst is additive, not a replacement: a release without a precise scheduled time, a connector tripped on cool-down, an ECB row, or a window-close exit still gets picked up by the next hourly pass.
- **P-sched-4 (shipped — health telemetry wiring):** `SourceCapabilityManager.get_customer_health()` now calls a new `_calendar_connectors_block()` helper that reads every row from `calendar_connector_state` via `scheduler_state.list_connector_states(conn)` and classifies each as `ok` / `failing` (pre-threshold) / `cooling` / `budget_exhausted`. The dashboard dict grew a top-level `calendar_connectors: [...]` array plus three summary counters (`calendar_cooling` / `calendar_budget_exhausted` / `calendar_failing`); any cooling or budget-exhausted connector promotes `healthy` → `degraded` so `/health` consumers notice a stale forward calendar without parsing the nested block. Never demotes `unhealthy` (every source failing) to `degraded` — calendar telemetry is additive, not a substitute signal. Early-warning `failing` (partial consecutive failures below the cool-down threshold) does NOT flip the top-level status — it's surfaced in the summary counter only so alerting stays anchored on the breaker / budget trip, not transient flakiness. The block's read is wrapped in `contextlib.closing` so polled `/health` calls don't leak SQLite handles, and is exception-safe so a missing migration never 500's the health endpoint. 6 new tests covering empty / cooling / budget-exhausted / failing / ok-with-row paths plus the unhealthy-preservation invariant.
- **P6 (shipped — cross-provider parity harness):** `src/ingestion/calendar/parity.py` — pure computation over `cal_econ_event`. `calendar_econ_parity(connection, from_date, to_date, indicators=?)` buckets rows by `(country_code, canonicalize_indicator(title), reference_date)` inside the `(from_date, to_date)` window on `event_time_utc`, groups by provider inside each bucket, and classifies each bucket as `matched` (TE + at least one official provider present), `te_only` (scheduler missed a release TE carries — actionable), or `official_only` (TE missed one the scheduler caught — documented blind spot, e.g. the NBS MOF/PBOC caveat from the P6 ask). Multi-revision rows on one side collapse to a single provider-presence — three TE revisions of the same CPI release count as one TE-side presence, not three. Title variation ("Consumer Price Index" vs "CPI") collapses through the shared `_official_shared.canonicalize_indicator` alias table, so BLS and TE rows for the same release converge. `format_parity_report(summary)` renders a markdown block suitable for checking into `docs/validation/calendar_parity_<date>.md`; per-event gap lists are capped at 50 entries with a tail marker so the report stays review-friendly. Service op `calendar_econ_parity(from_date, to_date, indicators?, write_report=False)` exposes both the computation and the optional report write. The service op's DB read is wrapped in `contextlib.closing` (same reason as P-sched-4). `TE_PROVIDER = "tradingeconomics"` and `OFFICIAL_PROVIDERS = ("bls", "bea", "census", "ism", "umich", "conference-board", "nar", "ecb", "eurostat", "destatis", "zew", "insee", "ine", "istat", "federal-reserve", "nbs", "mof-jp", "cao", "meti", "stat-bureau-jp")` are module-level constants; a regression test pins them against each connector's `parser.PROVIDER` so a drift zero-matches the report loudly. 14 new tests, no real upstream HTTP in CI.
- **Issue #15 P1 (shipped — Eurostat connector):** `src/ingestion/calendar/eurostat_api/` adds Eurostat as provider id `eurostat` (`provider_type=government_agency`, `cal_provider.precedence=100`). Value side uses the existing `EurostatClient` JSON-stat path against `ec.europa.eu/eurostat/api/dissemination/statistics/1.0/data/` for three Euro-area anchors: HICP flash annual rate (`prc_hicp_fpd`, `unit=RCH_A`, `coicop18=TOTAL`, `release=FLS`, `geo=EA20`), GDP flash QoQ (`namq_10_gdp`, `na_item=B1GQ`, `unit=CLV_PCH_PRE`, `s_adj=SCA`, `geo=EA20`), and unemployment rate (`une_rt_m`, `unit=PC_ACT`, `age=TOTAL`, `sex=T`, `s_adj=SA`, `geo=EA20`). Schedule side consumes Eurostat's release-calendar JSON feed (`/eurostat/o/calendars/eventsJson`) with Economy and finance / Euro indicators filters, using Europe/Luxembourg release-clock semantics. Schedule rows and value rows share `provider_event_id` on `(provider, country, canonical indicator, reference_date)` so `project_schedule_events` owns the exact release datetime while `project_events` fills `actual`. Service ops: `calendar_econ_fetch_eurostat` and `calendar_econ_schedule_eurostat`; recurring scheduler includes `eurostat` in both schedule and value-side rosters. Focused mocked tests cover registry coordinates, parser projection, release-calendar JSON parsing, schedule/value merge, service dry-run, provider seeding, parity roster, scheduler integration, and shared projector compatibility.
- **Issue #15 P2 (shipped — Destatis connector):** `src/ingestion/calendar/destatis_api/` adds Destatis as provider id `destatis` (`provider_type=government_agency`, `cal_provider.precedence=100`) and observation source id `destatis`. Value side uses the POST-based GENESIS-Online tablefile endpoint (`/genesisWS/rest/2020/data/tablefile`) with `username` / `password` headers and env fallbacks `DESTATIS_GENESIS_USERNAME` / `DESTATIS_GENESIS_PASSWORD`; the minimum slice ships two German anchors: CPI preliminary YoY (`61111-0004`, matched on Gesamtindex / Vorjahreszeitraum) and GDP flash QoQ (`81000-0001`, matched on price-adjusted seasonally/calendar-adjusted Vorquartal rows). Schedule side scrapes Destatis' weekly release table (`/DE/Presse/Termine/Veroeffentlichungstabelle/_inhalt.html`), using Europe/Berlin release clocks, 10:00 Uhr row overrides, and date precision for CPI rows marked "im Laufe des Tages". Schedule rows and value rows share `provider_event_id` on `(provider, country, canonical indicator, reference_date)`; value fetch preserves already-staged schedule precision when the GENESIS row lands. Service ops: `calendar_econ_fetch_destatis` and `calendar_econ_schedule_destatis`; recurring scheduler includes `destatis` in both schedule and value-side rosters. Focused mocked tests cover GENESIS POST shape, CSV projection, release-table parsing, schedule/value ID convergence, service dry-runs, provider seeding, parity roster, and scheduler integration.
- **Issue #15 P3a (shipped — Spain INE connector):** `src/ingestion/calendar/ine_api/` adds Spain's Instituto Nacional de Estadistica as provider id `ine` (`provider_type=government_agency`, `cal_provider.precedence=100`) and observation source id `ine`. Schedule side consumes INE's full-year publication calendar (`/dynt3/Calendario/es/calenHTML.htm`) and anchors the first release per quarterly national-accounts reference as the GDP advance row; CPI keeps only `Avance.` rows from the Consumer Price Index calendar section. Stored release times use Europe/Madrid DST-aware conversion at 09:00. Value side auto-discovers due INE rows with `actual IS NULL` and fetches the stable official press-release slugs (`adIPCMMYY.htm` for CPI advance and `avCNTRQTYY.htm` for GDP advance), parsing the headline YoY CPI advance and QoQ GDP advance values. Schedule rows and value rows share `provider_event_id` on `(provider, ES, canonical indicator, reference_date)`. Service ops: `calendar_econ_schedule_ine` and `calendar_econ_fetch_ine`; recurring scheduler includes `ine` in both schedule and value-side rosters. Focused mocked tests cover calendar parsing, GDP advance de-duplication, press URL synthesis, press-value extraction, schedule/value ID convergence, service dry-runs, provider seeding, parity roster, scheduler integration, and shared projector compatibility.
- **Issue #15 P3b (shipped — Italy ISTAT connector):** `src/ingestion/calendar/istat_api/` adds Italy's national statistical institute as provider id `istat` (`provider_type=government_agency`, `cal_provider.precedence=100`) and observation source id `istat`. Schedule side discovers/extracts ISTAT's annual English press-release calendar PDF and parses CPI `Consumer prices P` rows plus `Preliminary estimate of GDP` rows with Europe/Rome DST-aware release times. Value side auto-discovers due ISTAT rows with `actual IS NULL`, fetches the official English press-release slugs (`consumer-prices-provisional-data-<month>-<year>/` and `preliminary-estimate-of-gdp-q<q>-<year>/`), and parses headline CPI YoY and GDP QoQ values. Schedule rows and value rows share `provider_event_id` on `(provider, IT, canonical indicator, reference_date)`. Service ops: `calendar_econ_schedule_istat` and `calendar_econ_fetch_istat`; recurring scheduler includes `istat` in both schedule and value-side rosters. Focused mocked tests cover PDF-text calendar parsing, press URL synthesis, press-value extraction, schedule/value ID convergence, service dry-runs, provider seeding, parity roster, scheduler integration, and shared projector compatibility.
- **Issue #15 P3c (shipped — France INSEE connector):** `src/ingestion/calendar/insee_api/` adds France's national statistical institute as provider id `insee` (`provider_type=government_agency`, `cal_provider.precedence=100`) and observation source id `insee`. Schedule side consumes INSEE's agenda JSON endpoint (`/en/agenda-diffusion`) and filters family ids `1250` (CPI provisional) and `1251` (GDP first estimate), storing Europe/Paris-aware release datetimes. Value side auto-discovers due INSEE rows with `actual IS NULL`, resolves the official release page through `/en/solr/consultation` filtered by `familleId`, and parses headline CPI YoY and GDP QoQ values from the English press-release HTML. Schedule rows and value rows share `provider_event_id` on `(provider, FR, canonical indicator, reference_date)`. Service ops: `calendar_econ_schedule_insee` and `calendar_econ_fetch_insee`; recurring scheduler includes `insee` in both schedule and value-side rosters. Focused mocked tests cover agenda JSON parsing, search-result resolution, press-value extraction, schedule/value ID convergence, service dry-runs, provider seeding, parity roster, scheduler integration, capability discovery, and shared projector compatibility.
- **Issue #15 P4a (shipped — Germany ZEW connector):** `src/ingestion/calendar/zew_api/` adds ZEW as provider id `zew` (`provider_type=market_data`, `cal_provider.precedence=100`) and observation source id `zew`. Schedule side consumes ZEW's annual release-dates page for the ZEW Indicator of Economic Sentiment and stores Europe/Berlin-aware 11:05 Frankfurt release datetimes. Value side auto-discovers due ZEW rows with `actual IS NULL`, resolves the matching monthly press-release page from the latest press-release listing, and parses the headline Germany economic-sentiment index level from the English HTML. Schedule rows and value rows share `provider_event_id` on `(provider, DE, ZEW_SENTIMENT, reference_date)`. Service ops: `calendar_econ_schedule_zew` and `calendar_econ_fetch_zew`; recurring scheduler includes `zew` in both schedule and value-side rosters. Focused mocked tests cover release-date parsing, DST conversion, listing-page resolution, press-value extraction, schedule/value ID convergence, service dry-runs, provider seeding, parity roster, scheduler integration, capability discovery, and shared projector compatibility.
- **Issue #15 P4b (shipped — Germany Ifo connector):** `src/ingestion/calendar/ifo_api/` adds Ifo Institute as provider id `ifo` (`provider_type=market_data`, `cal_provider.precedence=100`) and observation source id `ifo`. Schedule side consumes Ifo's English Business Climate survey page (`/en/survey/ifo-business-climate-index-germany`), extracting `<day> <Month> <Year> HH:MM CET/CEST` entries and defaulting to 10:30 Europe/Berlin when the time is elided. Value side auto-discovers due Ifo rows with `actual IS NULL`, resolves the monthly press-release link (`/en/press-release/YYYY-MM-DD/<slug>`) from the English press landing page by matching the release-date path segment, and parses the headline index level (e.g. `"fell to 84.4 points"` → `84.4`) from the press HTML. Schedule rows and value rows share `provider_event_id` on `(provider, DE, IFO_BUSINESS_CLIMATE, reference_date)`. Service ops: `calendar_econ_schedule_ifo` and `calendar_econ_fetch_ifo`; recurring scheduler includes `ifo` in both schedule and value-side rosters. Schedule refresh preserves the resolved `/en/press-release/` URL after value fill. Focused mocked tests cover survey-page parsing, listing-page resolution, press-value extraction, schedule/value ID convergence, service dry-runs, provider seeding, parity roster, scheduler integration, capability discovery, and shared projector compatibility.
- **Issue #15 P5 + Issue #23 (shipped — Germany HCOB / S&P Global PMI connector, schedule + values):** `src/ingestion/calendar/hcob_api/` adds Germany Flash / final Manufacturing / final Services PMIs as provider id `hcob` (TE-compatible; `provider_type=market_data`, `cal_provider.precedence=100`) and observation source id `hcob`. Although the public S&P Global PMI release-dates page (`pmi.spglobal.com/Public/Release/ReleaseDates?language=en`) labels the rows as `S&P Global Flash Germany PMI` / `S&P Global Germany Manufacturing PMI` / `S&P Global Germany Services PMI`, the survey has been commissioned by HCOB (Hamburg Commercial Bank) since the 2023 Markit rebrand; we store the rows under `HCOB` to match TE parity. Schedule side parses the calendar's `listSubHeading` / `listItem` DOM pairs (month-day headings with no year — resolved against today's UTC date, rolling over to next year for any date already in the past), extracts the `HH:MM UTC` clock, and projects rows per indicator per month. Flash publishes on the 3rd-last business day at 07:30 UTC; final Manufacturing at 07:55 UTC on the 1st business day of month N+1; final Services / Composite at 07:55 UTC on the 3rd business day of month N+1. Reference-date anchors on the release month for flash and on month N-1 for the final releases (reporting month). **Flash trio fan-out:** the single upstream "S&P Global Flash Germany PMI" calendar row expands into three series at projection time (`HCOB_FLASH_MANUFACTURING_PMI`, `HCOB_FLASH_SERVICES_PMI`, `HCOB_FLASH_COMPOSITE_PMI`) — both because the flash PDF carries three headline indices and because TE ships three per-component flash rows that would otherwise collapse onto the final-release canonical tokens via the shared `" flash"` suffix-stripper. New canonicalize aliases register the flash variants pre-strip so the parity harness buckets them together with TE's rows. **Value side (issue #23):** the public press-release listing (`pmi.spglobal.com/Public/Release/PressReleases?language=en`) ships GUID-keyed PDF URLs alongside `(date, UTC time, title)` markup; `fetch_hcob_calendar` auto-discovers `actual IS NULL` rows, resolves each release's PDF URL by matching `(release_date, expected_listing_title)` (skipping the `(Deutsch)` German-language siblings), downloads the PDF (served as `Content-Type: application/pdf`), extracts text via `pypdf`, and runs per-series regexes — flash trio shares one PDF (cached per URL across the three series so each download happens once per sweep). Schedule rows and value rows share `provider_event_id` on `(provider, DE, canonical_indicator, reference_date)`. Service ops: `calendar_econ_schedule_hcob` and `calendar_econ_fetch_hcob`; recurring scheduler includes `hcob` in both schedule and value-side rosters. Focused mocked tests cover calendar-DOM walking, year resolution for forward-looking pages, flash vs. final reference-date semantics, flash-trio fan-out, indicator title mapping, schedule/value ID convergence, listing-resolver English-vs-German selection, per-series PDF extraction (including tolerance for an actual upstream typo "came it at" in the services PDF), end-to-end value-sweep upserts, per-series failure isolation, service dry-runs, provider seeding, parity roster, scheduler integration, capability discovery, and shared projector compatibility.
- **Issue #24 (shipped — European Commission BCS connector):** `src/ingestion/calendar/ec_bcs_api/` adds the European Commission DG ECFIN Business and Consumer Surveys programme as provider id `ec-bcs` (`provider_type=government_agency`, `cal_provider.precedence=100`, country `EU`) and observation source id `ec-bcs`. Two anchor indicators: `EC_BCS_ESI` ("Euro Area Economic Sentiment Indicator", monthly ~end of month at 11:00 Europe/Brussels, part of the Business and consumer survey results bulletin) and `EC_BCS_CCI_FLASH` ("Euro Area Consumer Confidence Flash", monthly ~22nd at 16:00 Europe/Brussels). Schedule side discovers the annual `Publication dates <YYYY>.pdf` link from the survey landing page (`economy-finance.ec.europa.eu/economic-forecast-and-surveys/business-and-consumer-surveys_en`), extracts the PDF text via `pypdf` (with `pdftotext` / `mutool` fallback), and parses each `DD <Month> YYYY HHhMM` token — release time discriminates the two streams (`16h00` → Flash CCI, `11h00` → ESI/full-survey). The December full-survey row publishes on the next-January date; the parser anchors that row's reference month on the prior December. Value side auto-discovers due rows with `actual IS NULL`, resolves the matching press-release PDF (`economy-finance.ec.europa.eu/document/download/<UUID>_en`) from the press-releases listing page by matching the indicator label and the release month/date, and parses the headline euro-area aggregate from the extracted PDF text — ESI matches `"Economic Sentiment Indicator … euro area … to <value>"`, Flash CCI matches `"At <EU> (EU) and <euro-area> (euro area) points"`. Schedule rows and value rows share `provider_event_id` on `(provider, EU, EC_BCS_ESI | EC_BCS_CCI_FLASH, reference_date)`. Service ops: `calendar_econ_schedule_ec_bcs` and `calendar_econ_fetch_ec_bcs`; recurring scheduler includes `ec-bcs` in both schedule and value-side rosters. Canonical-indicator alias-table now checks the alias before stripping modifier suffixes so `Consumer Confidence Flash` resolves to `EC_BCS_CCI_FLASH` rather than collapsing to `CB_CONSUMER_CONFIDENCE` after a `" flash"` strip. Sub-index fan-out (Industrial / Services / Retail / Construction / Consumer Confidence final) and country-level breakouts (DE / FR / IT / ES) are out of scope for this slice.
- **Issue #15 P4c (shipped — Germany GfK / NIM Consumer Climate connector):** `src/ingestion/calendar/gfk_api/` adds the GfK Consumer Climate study (now published by NIM — Nürnberg Institute for Market Decisions, branded "NIM Consumer Climate powered by GfK") as provider id `gfk` (TE-compatible; `provider_type=market_data`, `cal_provider.precedence=100`) and observation source id `gfk`. Schedule side consumes the English Consumer Climate page (`nim.org/en/consumer-climate`), scraping the inline "Planned publication dates (CET)" block lines like `Monday, April 27, 2026, 8:00 a.m.` and mapping to 8:00 Europe/Berlin (default when the time suffix is dropped). Value side auto-discovers due GfK rows with `actual IS NULL`, resolves the matching press release from the all-releases listing (`/en/consumer-climate/all-releases`) by matching the printed `Month Day, Year` against the release date, and parses the headline index level from the press HTML (e.g. `"Consumer Climate indicator for April 2026 falls to -28.0 points"` → `-28.0`). Reference-date anchors on the first day of the release month (matches TE's per-release labeling for forward-looking indicators); the "forecast for next month" semantics rides on the release title and raw payload. Schedule rows and value rows share `provider_event_id` on `(provider, DE, GFK_CONSUMER_CLIMATE, reference_date)`. Service ops: `calendar_econ_schedule_gfk` and `calendar_econ_fetch_gfk`; recurring scheduler includes `gfk` in both schedule and value-side rosters. Schedule refresh preserves the resolved `/en/consumer-climate/detail-consumer-climate/` URL after value fill. Focused mocked tests cover planned-date parsing, listing-page resolution, press-value extraction (including negative indicator levels), schedule/value ID convergence, service dry-runs, provider seeding, parity roster, scheduler integration, capability discovery, and shared projector compatibility.
- **P3b-live (shipped — ECB SDMX live run):** `scripts/validate_calendar_acquisition.py --provider ecb --execute` hit the ECB Data Portal for three probes (MRO / DFR / MLF over a 730-day window). All three returned clean — 8 rate-change rows per series after rate-level collapse, zero UNKNOWN_OBSERVED / MISSING_EXPECTED, parser dry-parse 8/8 per probe. Report at `docs/validation/calendar_acquisition_ecb_2026-04-22.md`. Docs-only slice — no connector code changes.
- **P2b-live (shipped — BEA live run + whitelist expansion):** `--provider bea --execute` ran four probes clean; two whitelist additions landed with `(table, line)` verified against the live response: **PCE Price Index** (NIPA `T20804` L1, monthly, inflation-headline of the Personal Income and Outlays release) and **Corporate Profits** (NIPA `T11200` L13, quarterly, publishes with GDP second/third estimates). Trade Balance deferred — the issue-body hint called for the ITA dataset, but ITA exposes only A / QSA / QNSA frequencies and uses a different parameter surface (`Indicator` + `AreaOrCountry`, no `LineNumber`), so shipping it requires a client extension; the NIPA near-equivalent `T10105` L15 "Net exports of goods and services" is quarterly *annualized* from the GDP accounting — wrong semantics and scale for the monthly Trade Balance release. The live run also surfaced BEA schedule-page drift (the `Release Date` column header dropped; `parse_schedule_html` no longer matches the 2026 layout) — tracked as a separate follow-up distinct from this slice. Report at `docs/validation/calendar_acquisition_bea_2026-04-22.md`.
- **P5c-live (shipped — NBS live run + whitelist expansion):** `--provider nbs --execute` hit stats.gov.cn (index page + 2026 yearly article) and parsed 81 entries across seven indicators. Whitelist additions with `label_fragment` verified against the live 2026 calendar: **PPI**, **Industrial Production**, **Fixed Asset Investment**, **Retail Sales**, **Manufacturing PMI**, **Non-Manufacturing PMI**. The two PMI specs share the "purchasing managers" `label_fragment` because NBS publishes both measures under one calendar row; the scraper's former "first match wins" behaviour changed to `_row_matching_indicators(...) -> list[NBSIndicatorSpec]` so every matching spec emits one entry per release date. GDP deferred — NBS publishes quarterly GDP inside the monthly "National Economic Performance" row (no dedicated GDP row), so a naive match would emit 11 events/year vs the 4 quarterly GDPs traders watch; quarterly-month filtering belongs in a separate slice. Two live-run fixes folded in: **UTF-8 encoding override on both NBS fetchers** (the server omits `charset` from `Content-Type`, forcing `requests` onto the RFC 2616 ISO-8859-1 fallback, which mis-decoded the empty-day marker), and **multi-token day cells** (`_parse_day_cell` now returns every `day/weekday` token — the March PMI cell ``"4/Wed Note5 31/Tue"`` carries both the Spring-Festival-delayed Feb release and the regular Mar 31 release). Report at `docs/validation/calendar_acquisition_nbs_2026-04-22.md`.
- **P4b-live (partially shipped — Fed FOMC probe + upstream drift surfaced):** `--provider fed --execute` ran both planned probes; the FOMC-calendar probe lands clean (56 meeting rows, 2021-2027) after a small parser fix — the `fomccalendars.htm` page now carries a `"22 (notation vote)"` row for August 2025, an inter-meeting email vote that's NOT a rate-decision meeting, so `parse_fomc_calendar_html` now skips any date cell containing "notation vote" rather than raising. The `releasedates.htm` probe failed with a real 404 — the Fed retired the HTML page and published the same data at `/json/calendar.json` (structured JSON feed exposing Beige Book / H.4.1 / H.8 events alongside FOMC meetings, speeches, testimony). Migrating `parse_releasedates_html` from HTML scraping to JSON consumption is the P4b-live-follow-up slice (non-trivial rewrite plus fixture + tests regen — held separate from the scope-constrained live-run shipment). `render_report` also gained a probe-level-failure signal so the summary no longer claims "No scaffold changes required" when every probe 404'd (observed on the first Fed run before the notation-vote fix). Report at `docs/validation/calendar_acquisition_fed_2026-04-22.md`.
- **P4b-live-follow-up (shipped — Fed calendar JSON migration):** `src/ingestion/calendar/fed_api/releases.py` replaced HTML scraping of the 404'd `/newsevents/releasedates.htm` with JSON consumption of `/json/calendar.json`. The constant renames land accordingly (`FED_RELEASEDATES_URL` → `FED_CALENDAR_JSON_URL`; `parse_releasedates_html` / `fetch_releasedates_html` / `FedReleasedatesParseError` → `parse_fed_calendar_json` / `fetch_fed_calendar_json` / `FedCalendarJsonParseError`), while `FedReleaseEntry` / `release_entry_to_records` / `fetch_fed_releasedates` / `ReleasedatesRunSummary` keep their names — the output semantics (Beige Book / H.4.1 / H.8 calendar rows) and downstream shape are unchanged. The JSON feed ships with a UTF-8 BOM that `requests.text` mis-decodes; the fetcher decodes from raw bytes via `utf-8-sig`, and the parser strips any stray leading BOM defensively. Month-level events with comma-separated `days` strings (`"3, 10, 17, 24, 31"`) fan out to one :class:`FedReleaseEntry` per day; multi-day weekly releases now land correctly instead of collapsing to the first date per month. The JSON endpoint accepts the default `python-requests` UA (no browser headers) — only the FOMC and statement HTML surfaces keep the `_FED_BROWSER_HEADERS` bundle. Fixture at `tests/fixtures/fed_releasedates/calendar.json`; test file renamed functions / assertions to match. Live probe (`--provider fed --execute`) now lands both surfaces clean — pending operator re-run for a fresh checked-in report.
- **P5c-gdp (shipped — NBS GDP added with quarterly cadence):** `NBSIndicatorSpec` gained two optional fields — `publishing_months: frozenset[int] | None` (emit entries only for months in the set) and `reference_cadence: "monthly" | "quarterly"`. GDP joins the P5c whitelist under `label_fragment="national economic performance"`, `publishing_months=frozenset({1, 4, 7, 10})`, and `reference_cadence="quarterly"`. The "National Economic Performance" row carries 11 release dates per year (every month except Feb); the filter keeps only Jan / Apr / Jul / Oct (the quarterly GDP releases per NBS 2026 calendar note 3), dropping the seven monthly roll-up dates that ride the same row. The projector's new `_quarterly_reference` branch anchors each GDP event on end-of-prior-quarter (Jan 2026 release → `2025-12-31` / `"2025-Q4"`, Apr 2026 release → `2026-03-31` / `"2026-Q1"`, and so on) so downstream parity buckets compare against TE's quarterly convention cleanly. Monthly indicators (CPI, PPI, IP, FAI, Retail Sales, PMIs) stay untouched — they still anchor on first-of-release-month with `"YYYY-MM"` labels. Live NBS probe on 2026-04-22 now lands 85 entries (was 81): +4 GDP events with quarterly reference dates / labels.
- **P5d (shipped — NBS value-side fetcher, issue #49):** `src/ingestion/calendar/nbs_api/value_listing.py` + `value_parser.py` plus a new `fetch_nbs_values()` driver close the value gap on the China provider. The English NBS press-release listing (`https://www.stats.gov.cn/english/PressRelease/`) is fetched once per sweep, parsed into `(release_date, title, url)` triples, and matched against pending `actual IS NULL` schedule rows via a per-indicator `listing_title_fragment` declared on `NBSIndicatorSpec`. Each matched row downloads its press-release article; per-indicator regex extracts the YoY headline from the opening narrative paragraph (`"Consumer Price Index (CPI) increased by 1.0% year on year"`, `"to a 0.5% increase"` for PPI's previous-month comparison, `"value added of industrial enterprises … increased by 5.7%"`, `"year-on-year increase of 1.7%"` for FAI YTD, `"up by 1.7% year on year"` for Retail Sales). The direction word (`increased` / `decreased` / `decline`) signs the magnitude so a negative print isn't silently dropped. Five value-side indicators ship: CPI / PPI / Industrial Production / Fixed Asset Investment / Retail Sales — units flip from `index` to `percent_yoy` on the three that previously carried the schedule-only legacy. PMI / GDP / Manufacturing PMI / Non-Manufacturing PMI stay schedule-only: PMI articles aren't reliably surfaced on the English press-release listing and GDP uses a table-format parser, both deferred to a P5e slice. New scheduler entry `nbs-values` joins `ALL_VALUE_SIDE_CONNECTORS` with a five-title `_VALUE_SIDE_DUE_ROW_FILTERS` predicate so the schedule-aware burst picks up just-published rows within minutes. The NBS *schedule* writer (`fetch_nbs_calendar`) switches from `project_events` to `project_schedule_events` so a daily schedule re-scrape after a value sweep doesn't blank the `actual` the value side just filled. NBS stays out of `agency_registry.py` for now — schedule rows still anchor `reference_date` on the release month while TE buckets on the data month; wiring NBS into the parity registry without first aligning the reference anchor would produce false-positive alerts. The follow-up slice realigns the anchor and re-introduces the agency declaration. Service op `calendar_econ_fetch_nbs_values(dry_run)` exposes the driver. 21 mocked tests against captured fixture HTML; no real NBS HTTP in CI. Listing-page pagination is intentionally out of scope — the first page covers the burst window plus the daily catch-up sweep; older backfill remains a manual op.
- **Brazil coverage (shipped — issue #84, IBGE + Banco Central do Brasil):** Two new official-source connectors close the Brazil gap (TE: 4405 events historically; the largest uncovered G20/EM country flagged by the 2026-04-26 coverage audit on epic #57). **IBGE** (`src/ingestion/calendar/ibge_api/`): the Instituto Brasileiro de Geografia e Estatística release calendar at `ibge.gov.br/calendario/mensal.html?mes=N&ano=YYYY` is server-rendered HTML carrying every scheduled and recently-published statistical release for the requested month. Each event row sits inside a single `<li>` block pairing `<span data-divulgacao="YYYY-MM-DD HH:MM:SS-03:00">` (release moment with São Paulo offset baked in — Brazil dropped DST in 2019), a product-anchored `<a href="..." data-produto-id='NNNN'>{title}</a>` link, and an optional `Período de referência: M/YYYY` line (range form `"10/2025 a 12/2025"` for quarterly releases). The fetcher walks a rolling forward window (current month + next three) so a daily sweep keeps the calendar's lookahead fresh. Indicator matching anchors on `data-produto-id` rather than title-substring — IPCA (9256), IPCA-15 (9260), IPCA Especial (9270) all share the `"Índice Nacional de Preços ao Consumidor Amplo"` prefix, so substring matching would false-match without an exclusion list. Five indicators ship in P1: IPCA (`CPI`, pid 9256), IPCA-15 (`IPCA_15`, pid 9260), PIM-PF Brasil (`INDUSTRIAL_PRODUCTION`, pid 9294), PNAD-Contínua Mensal (`UNEMPLOYMENT_RATE`, pid 9171), Sistema de Contas Nacionais Trimestrais (`GDP`, pid 9300). The `_li_block_re → _divulg_attr_re / _pid_link_re / _ref_period_line_re` two-pass parser walks `<li>` boundaries first, then extracts row-local fields with bounded sub-regexes — avoids the cross-row bleed that a single combined regex hits when an empty `Período de referência` line silently consumes the next row's content under DOTALL. Quarterly references are written as a month range (`"10/2025 a 12/2025"` for Q4 2025); the projector parses both single-bucket and range forms and maps the result to the spec's frequency. **Schedule-only slice** — events publish with `actual=NULL`. The per-release press-release pages reachable from each event's `<a href>` carry the value side; the value-side scrape is deferred to P2 (mirrors the ABS / KOSTAT schedule-only pattern). **BCB** (`src/ingestion/calendar/bcb_api/`): the Banco Central do Brasil exposes the full Copom (Comitê de Política Monetária) decision history at `bcb.gov.br/api/servico/sitebcb/historicotaxasjuros` as a single JSON object `{"conteudo": [...]}` with one element per meeting since #1 (26 June 1996; ~285 decisions at capture time). Each element carries `NumeroReuniaoCopom`, `ReuniaoExtraordinaria` (3 in the historical record), `DataReuniaoCopom` / `DataInicioVigencia` / `DataFimVigencia` (encoded as São Paulo midnights on a UTC offset — only the date portion is meaningful), `Vies`, `MetaSelic` (the new target rate, e.g. `14.75`), and `descisaoMonocraticaPres` (8 monocratic presidential decisions in the historical record). The parser walks every element, anchors `previous_rate` on the chronologically-prior decision, and projects every Copom announcement (change OR hold OR extraordinary OR monocratic-presidential) as one calendar event at **18:30 BRT** (the BCB's documented post-meeting publication time); `parse_scheduled_release_time(default_tz="America/Sao_Paulo")` resolves DST so historical 2008-2018 backfill rows land at the correct UTC offset. Unlike the BoC Valet pattern, hold rows are present in the JSON (each meeting carries `MetaSelic` even when unchanged), so the parity whitelist is achievable in P1 without a separate fixed-meeting feed. **Schedule + value slice** — `actual = MetaSelic`, `previous = prior decision's MetaSelic`. Both connectors join `ALL_CONNECTORS` + `ALL_VALUE_SIDE_CONNECTORS`; both write rows post-publication and fall through to the hourly baseline rather than carrying a burst predicate (matches the EIA / DOL / ONS / BoE / StatCan / BoC / ABS / RBA / MoSPI / RBI / KOSTAT / BOK pattern). `agency_registry.py` adds `IBGE` (provider attribution only — schedule-only `actual=NULL` ships, so wiring `(BR, CPI / IPCA_15 / INDUSTRIAL_PRODUCTION / UNEMPLOYMENT_RATE / GDP)` into the daily parity tripwire would trip the parse_failed-on-missing-actual path on every release; same deferral pattern as the ABS / MoSPI / KOSTAT slices) and `BCB` (parity whitelist `(BR, BCB_RATE)` — the JSON service exposes target Selic inline, so the daily comparator can rely on matched rows for every Copom announcement). Country alias map gains `BR` / `BRA` / `BRAZIL` / `BRASIL` → `BR` and the `Brazil` display label. Canonicalize aliases cover `Brazil Inflation Rate` / `Brazilian CPI` / `IPCA` / `Brazil IPCA-15` / `Brazil GDP Growth Rate` / `Sistema de Contas Nacionais Trimestrais` / `Brazil Industrial Production` / `Pesquisa Industrial Mensal: Produção Física` / `Brazil Unemployment Rate` / `BCB Interest Rate Decision` / `Banco Central do Brasil Interest Rate Decision` / `Selic Rate` / `Selic Target Rate` / `Copom Interest Rate Decision` / `Meta Selic`. A new canonical token `IPCA_15` distinguishes the mid-month preview from the headline `CPI`. `cal_provider` seed grows to 41 entries with `ibge` typed `government_agency` and `bcb` typed `central_bank`. Fixtures captured live on 2026-04-27 in `tests/fixtures/ibge_release_calendar/release_2026_03.html` (March 2026 calendar including the quarterly PIB release) + `release_2026_04.html` (April 2026 calendar covering IPCA / IPCA-15 / PIM-PF / PNAD-CM) and `tests/fixtures/bcb_copom/historicotaxasjuros.json` (full Copom history — 285 decisions back to 1996); 33 mocked tests cover HTML parser correctness across li-block boundary / multi-month walk / IPCA-vs-IPCA-15 priority / quarterly-range-period / missing-line / zero-row shapes, JSON parser correctness across missing-conteudo / unparseable-JSON / zero-decisions / malformed-rate / chronological-previous shapes, BRT conversion (09:00 BRT → 12:00 UTC; 18:30 BRT → 21:30 UTC; pre-2019 18:30 BRST → 20:30 UTC), idempotency on re-sweep, no double-counting when one indicator's title is a prefix of another's, dry-run plan envelope, scheduler roster membership, agency attribution, and canonicalize aliases — no real IBGE / BCB HTTP in CI.
- **Korea coverage (shipped — issue #55, Statistics Korea + Bank of Korea):** Two new official-source connectors close the Korea gap (TE: 3524 events / 102 indicators historically). **KOSTAT** (`src/ingestion/calendar/kostat_api/`): Statistics Korea's annual release schedule lives at `mods.go.kr/menu.es?mid=a20301000000` (the `kostat.go.kr` domain redirects to `mods.go.kr` after the agency's 2025 rebrand to "Ministry of Data and Statistics"). The page is server-rendered HTML with a `<h3>{year} Schedule</h3>` heading anchoring every row in that year, and each row carries `<td class="span">{Mon abbr}.</td><td class="AGL">{title}</td><td>{Mon. DD (Day.)}</td><td>{division}</td>`. Three indicators ship in P1 — CPI (`"Consumer Price Index"` substring), Industrial Production (`"Monthly Industrial Statistics"`), Unemployment Rate (`"Economically Active Population Survey"`) — matched against `INDICATOR_REGISTRY`. The reference period is parsed from the title's `"... in <Month> <Year>"` suffix (so December 2025's release on Jan 14 2026 carries `reference_date=2025-12-01` while the page header anchors `release_date=2026-01-14`). Per-indicator KST release times: CPI / IP at 08:00, Employment at 12:00 (Korean SDDS conventional times). **Schedule-only slice** — events publish with `actual=NULL`. The page does not expose values directly; per-release values live behind a JS handler (`schdulPop('NNN')` posting to `view.json`); the value-side scrape is deferred to P2. **BOK** (`src/ingestion/calendar/bok_api/`): the Meeting Dates page at `bok.or.kr/eng/main/contents.do?menuNo=400020` embeds the inline `Schedule of the MPB's policy-setting meetings` block with one `<h3>YYYY</h3>` sub-table per scheduled / past year (2011 → current). Each year's table carries eight bi-monthly meeting cells like `"Jan.16 (Thu)"` (Jan / Feb / Apr / May / Jul / Aug / Oct / Nov — the MPB skips Mar / Jun / Sep / Dec). The parser anchors on the `<h2>Schedule of the MPB's policy-setting meetings</h2>` heading to ignore unrelated `<h3>YYYY</h3>` markers in page navigation, walks every year block, and extracts cells via a regex tolerant of `&nbsp;`-vs-space whitespace between day and paren-wrapped day-of-week. Each meeting projects as one calendar event anchored on the closing day at **09:50 KST** (the documented post-meeting Governor's Statement convention). **Schedule-only slice** — values stay `actual=NULL`; per-meeting Monetary Policy Decision press-release scrape (`"the Base Rate at X.YZ percent"`) is deferred to P2. Forward-year dates that BOK posts only as a `.hwp`/`.pdf` attachment (typical lag between fiscal-year close and the inline update) are silently absent until BOK adds the new `<h3>YYYY</h3>` block; the connector picks them up automatically on the next sweep. Both connectors join `ALL_CONNECTORS` + `ALL_VALUE_SIDE_CONNECTORS`; both write rows post-publication and fall through to the hourly baseline rather than carrying a burst predicate (matches the EIA / DOL / ONS / BoE / StatCan / BoC / ABS / RBA / MoSPI / RBI pattern). `agency_registry.py` adds `KOSTAT` (provider attribution only — schedule-only `actual=NULL` ships, so wiring `(KR, CPI / INDUSTRIAL_PRODUCTION / UNEMPLOYMENT_RATE)` into the daily parity tripwire would trip the parse_failed-on-missing-actual path on every release; same deferral pattern as the ABS / MoSPI slices) and `BOK` (provider attribution only — same rationale; `(KR, BOK_RATE)` joins the whitelist when the per-meeting Decision scrape lands in P2). Country alias map gains `KR` / `KOR` / `KOREA` / `SOUTH KOREA` / `REPUBLIC OF KOREA` → `KR` and the `South Korea` display label. Canonicalize aliases cover `South Korea Consumer Price Index` / `Korea Inflation Rate` / `South Korea Industrial Production` / `South Korea Unemployment Rate` / `BOK Interest Rate Decision` / `Bank of Korea Interest Rate Decision` / `Korea Base Rate`. KST already lives in `TIMEZONE_ALIASES` (`"KST" → "Asia/Seoul"`). `cal_provider` seed grows to 39 entries with `kostat` typed `government_agency` and `bok` typed `central_bank`. Fixtures captured live on 2026-04-27 in `tests/fixtures/kostat_release_calendar/release_schedule.html` (the 2026 schedule covering ~36 monthly publications across the three P1 families plus unrelated rows the substring matcher must skip) and `tests/fixtures/bok_meeting_dates/meeting_dates.html` (15 inline years × 8 meetings ≈ 120 MPB cells back to 2011); 34 mocked tests cover HTML parser correctness across heading-detection / cross-year-publication / nbsp-whitespace / nav-decoy / missing-heading / zero-row shapes, KST conversion (08:00 KST → 23:00 prior-day UTC; 09:50 KST → 00:50 UTC), fetcher per-indicator absence isolation, dry-run plan envelope, scheduler roster membership, agency attribution, and canonicalize aliases — no real KOSTAT / BOK HTTP in CI.
- **India coverage (shipped — issue #54, MoSPI + Reserve Bank of India):** Two new official-source connectors close the India gap (TE: 3976 events / 144 indicators historically). **MoSPI** (`src/ingestion/calendar/mospi_api/`): the Ministry of Statistics and Programme Implementation release calendar SPA at `mospi.gov.in/release-calendar` is backed by a JSON endpoint at `POST /api/release-calender/fetch-all-release-calender-Web` (body `{"lang": "en", "year": YYYY}`) returning ~30-100 schedule rows per year with `{id, title, description, doc_url, year, month, day, week, level}`. The fetcher iterates the requested years (default: current UTC year), parses each row, and substring-matches `title` against `INDICATOR_REGISTRY` (`"consumer price index"` / `"press release of cpi"` → `CPI`; `"index of industrial production"` / `"iip"` → `INDUSTRIAL_PRODUCTION`; `"gdp"` / `"gross domestic product"` / `"national accounts"` → `GDP`). Reference-date anchor uses a per-spec `reference_lag_months` field — CPI is M-1 (April release reports March data), IIP is M-2 (April release reports February data). Quarterly GDP anchors on the most recent quarter-end strictly before the release date; multi-stage GDP releases (First / Second / Third Advance Estimates + Provisional) for the same fiscal year quarter share the reference_date but synthesise distinct `provider_event_id`s by anchoring on the **release date** (each stage publishes on a different day). **Schedule-only slice** — events publish with `actual=NULL`. The MoSPI API does not expose values directly; PDF parsing of the per-release document linked in `description` is deferred to P2. The parity harness still buckets MoSPI rows against TE rows for presence matching. **RBI** (`src/ingestion/calendar/rbi_api/`): the `annualpolicy.aspx` page at `rbi.org.in/scripts/annualpolicy.aspx` embeds the inline "Meeting Schedule of the Monetary Policy Committee for YYYY-YYYY" press release listing six bi-monthly meeting date triples per fiscal year (`April 6, 7 and 8, 2026` → meeting closing day April 8). The parser tag-strips the page, locates the schedule heading, bounds the search at the next FY-archive widget marker, and extracts each triple via a regex tolerant of two-day shapes (`Month D1 [, D2] and D3, YYYY`). Each meeting projects as one calendar event anchored on the closing day at **10:00 IST** (RBI's documented post-meeting Governor's Statement convention). **Schedule-only slice** — values stay `actual=NULL`; per-meeting Resolution press-release scrape (`"policy repo rate at X.YZ percent"`) is deferred to P2. Both connectors join `ALL_CONNECTORS` + `ALL_VALUE_SIDE_CONNECTORS`; both write rows post-publication and fall through to the hourly baseline rather than carrying a burst predicate (matches the EIA / DOL / ONS / BoE / StatCan / BoC / ABS / RBA pattern). `agency_registry.py` adds `MOSPI` (provider attribution only — schedule-only `actual=NULL` ships, so wiring `(IN, CPI / INDUSTRIAL_PRODUCTION / GDP)` into the daily parity tripwire would trip the parse_failed-on-missing-actual path on every release; same deferral pattern as the ABS slice) and `RBI` (provider attribution only — same rationale; `(IN, RBI_RATE)` joins the whitelist when the per-meeting Resolution scrape lands in P2). Canonicalize aliases cover `India Inflation Rate` / `India CPI` / `All India Consumer Price Index` / `India GDP` / `India GDP Growth Rate` / `India Index of Industrial Production` / `India IIP` / `RBI Interest Rate Decision` / `Reserve Bank of India Interest Rate Decision` / `India Repo Rate` / `Policy Repo Rate`. `TIMEZONE_ALIASES` gains `"IST" → "Asia/Kolkata"`. `cal_provider` seed grows to 37 entries with `mospi` typed `government_agency` and `rbi` typed `central_bank`. Fixtures captured live on 2026-04-27 in `tests/fixtures/mospi_release_calendar/year_2026.json` (28 schedule rows: 4 CPI / 3 IIP / 2 GDP / ~19 unrelated PLFS / Infrastructure / Energy publications the substring matcher must skip) and `tests/fixtures/rbi_annualpolicy/annualpolicy.html` (FY 2026-2027 schedule with six MPC meetings); 26 mocked tests cover JSON parser correctness across non-success / zero-rows / undated-row / per-spec lag / quarter-end-anchor / multi-year-iteration shapes, RBI HTML schedule extraction across two-day-meeting / archive-widget-bounding / missing-heading / zero-triples shapes, IST conversion (10:00 IST → 04:30 UTC; 17:30 IST → 12:00 UTC), fetcher per-indicator absence isolation, dry-run plan envelope, scheduler roster membership, agency attribution, and canonicalize aliases — no real MoSPI / RBI HTTP in CI.
- **Australia coverage (shipped — issue #53, ABS + Reserve Bank of Australia):** Two new official-source connectors close the Australia gap (TE: 6694 events / 278 indicators historically). **ABS** (`src/ingestion/calendar/abs_api/`): the Australian Bureau of Statistics release calendar HTML at `abs.gov.au/release-calendar/latest-releases` is fetched once per sweep; each `views-row` card carries a UTC `<time datetime>` (ABS publishes at 11:30am AEST/AEDT and pre-converts to UTC in the markup), an indicator URL slug encoding the reference period (`mon-YYYY` for monthly, `<end-month>-YYYY` for quarterly), and a topic subtitle. The fetcher matches release-card URL prefixes against `INDICATOR_REGISTRY` (CPI / Labour Force headline → `UNEMPLOYMENT_RATE` / National Accounts → `GDP`); the `-detailed` Labour Force variant is intentionally excluded. **Schedule-only slice** — events publish with `actual=NULL`. The SDMX value lookup at `api.data.abs.gov.au` is deferred to P2 (per-indicator key parsing + quarterly-vs-monthly reference-date alignment is non-trivial); the parity harness still buckets ABS rows against TE rows for presence matching, which closes the immediate parity gap. **RBA** (`src/ingestion/calendar/rba_api/`): the Monetary Policy Board cash-rate-target HTML table at `rba.gov.au/statistics/cash-rate/` is parsed for every decision since August 1990 (~384 rows); each row pairs an announcement date with the new target rate and a signed change column. Unlike the BoC Valet pattern, **hold (no-change) decisions are present** as `0.00` rows — the parity whitelist is achievable in P1 without a separate fixed-announcement-dates feed. Announcement clock is 14:30 AEST/AEDT (the documented post-meeting publication time); `parse_scheduled_release_time(default_tz="Australia/Sydney")` resolves DST so the UTC timestamp is correct for both winter (UTC+10 → 04:30 UTC) and summer (UTC+11 → 03:30 UTC) announcements. Both connectors join `ALL_CONNECTORS` + `ALL_VALUE_SIDE_CONNECTORS`; both write rows post-publication and fall through to the hourly baseline rather than carrying a burst predicate (matches the EIA / DOL / ONS / BoE / StatCan / BoC pattern). `agency_registry.py` adds `ABS` (provider attribution only — the schedule-only slice ships with `actual=NULL`, so wiring `(AU, CPI / UNEMPLOYMENT_RATE / GDP)` into the daily parity tripwire would trip the parse_failed-on-missing-actual path on every release; same deferral pattern as the BoC slice) and `RBA` (parity whitelist `(AU, RBA_RATE)` — the cash-rate page publishes hold + change rows in the same shape, so the daily comparator can rely on matched rows for every scheduled MPB announcement). Country alias map gains `AU` / `AUS` / `AUSTRALIA` → `AU` and the `Australia` display label. Canonicalize aliases cover `Australia Inflation Rate` / `Australian CPI` / `Australia Unemployment Rate` / `Australia GDP Growth Rate` / `RBA Interest Rate Decision` / `Reserve Bank of Australia Interest Rate Decision` / `RBA Cash Rate Target`. Both upstream surfaces sit behind Akamai which 403s default-Python UA strings; the connectors use a browser-shaped `User-Agent` matching the DOL listing fetcher's pattern. Fixtures captured live on 2026-04-27 in `tests/fixtures/abs_release_calendar/latest_releases.html` (13 statistical release rows; the LF March 2026 row matched the whitelist) and `tests/fixtures/rba_cash_rate/cash_rate_table.html` (full MPB decisions table — 384 rows: 291 holds / 42 hikes / 51 cuts back to August 1990); 31 mocked tests cover parser correctness across malformed-row / missing-table / hold-row / synthetic-CPI/GDP / updated-information shapes, AEST/AEDT DST split, fetcher per-indicator absence isolation, dry-run plan envelope, scheduler roster membership, agency attribution, and canonicalize aliases — no real ABS / RBA HTTP in CI.
- **Canada coverage (shipped — issue #52, Statistics Canada + Bank of Canada):** Two new official-source connectors close the G7 Canada gap. **StatCan** (`src/ingestion/calendar/statcan_api/`): three indicators (CPI all-items index `V41690973`, Unemployment Rate `V2062815`, Monthly real GDP `V65201210`) hit `getDataFromVectorsAndLatestNPeriods` on the Web Data Service in a single batched POST per sweep; each observation carries `releaseTime` (Eastern Time wall clock) so schedule + value land together — the projector anchors `event_time_utc` on 08:30 ET via `parse_scheduled_release_time(default_tz="America/Toronto")`. **BoC** (`src/ingestion/calendar/boc_api/`): Valet observations sweep against `V39079` (Target for the Overnight Rate) detects rate-change days by walking the daily series chronologically and emitting one event per value transition; announcement clock is 09:45 ET. Valet's `d` is the first business day at the new rate, which lags the announcement by one business day (2025-10-29 announcement → 2025-10-30 in V39079); the parser pairs each transition with the prior parsed observation date as the announcement day, and the projector anchors `event_time_utc` / `provider_event_id` / `reference_date` on the announcement day so parity buckets line up with TE's "BoC Interest Rate Decision" rows (Bloomberg / Reuters convention). The Valet effective date is preserved verbatim in the audit payload. Hold (no-change) decisions are absent from the Valet signal — same slice cap as the BoE Bank-Rate page; capturing scheduled-but-no-change announcements via the BoC fixed-announcement-dates page is a follow-up. Both connectors join `ALL_CONNECTORS` + `ALL_VALUE_SIDE_CONNECTORS`; both write rows post-publication and fall through to the hourly baseline rather than carrying a burst predicate (matches the EIA / DOL / ONS / BoE pattern). `agency_registry.py` adds `STATCAN` (parity whitelist `(CA, UNEMPLOYMENT_RATE)` only — CPI is an index level and GDP is millions-CAD SAAR, both outside TE's rate display until a derivation step lands; same BLS pattern) and `BOC` (provider attribution only; parity whitelist deliberately empty — Valet's `V39079` exposes only rate-change days while TE carries every scheduled announcement including holds, so wiring `(CA, BOC_RATE)` into the daily parity tripwire before the BoC fixed-announcement-dates page is ingested would flag every hold day as a missing-release anomaly). Country alias map gains `CA` / `CAN` / `CANADA` → `CA` and the `Canada` display label. Canonicalize aliases cover `Canada Inflation Rate` / `Canadian CPI` / `Canada Unemployment Rate` / `Canada GDP Growth Rate` / `BoC Interest Rate Decision` / `Bank of Canada Interest Rate Decision` / `Target for the Overnight Rate`. Fixtures captured live on 2026-04-27 in `tests/fixtures/statcan_wds/` (3 vectors) and `tests/fixtures/boc_valet/` (overnight-rate observations spanning 9 rate changes since June 2024); 27 mocked tests cover parser correctness across batched / failed / suppressed / flat-history payloads, EDT/EST DST split, fetcher per-indicator failure isolation, dry-run plan envelope, scheduler roster membership, agency attribution, and canonicalize aliases — no real StatCan / BoC HTTP in CI.
- **US weekly high-frequency (shipped — issue #50, DOL UI Claims + EIA weekly stocks):** Two new official-source connectors close TE's largest US weekly-cadence gap. **EIA** (`src/ingestion/calendar/eia_api/`): pulls four weekly stocks series — Crude Oil (excl. SPR, `WCESTUS1`), Gasoline (`WGTSTUS1`), Distillate (`WDISTUS1`), Natural Gas Storage (`NW2_EPG0_SWO_NUS_BCF`) — through the existing `EIAClient.get_series` (auth via `EIA_API_KEY`). The JSON response carries the period (week-ending date) alongside the value, so `fetch_eia_calendar` writes schedule + value in one pass; the projector's `provider_event_id` anchors on the period for parity-bucket alignment. Crude/Gasoline/Distillate publish Wednesday 10:30 ET; Natural Gas publishes Thursday 10:30 ET — `_next_release_datetime` advances from the period date to the next configured DOW. **DOL** (`src/ingestion/calendar/dol_api/`): walks the `dol.gov/newsroom/releases/eta?lang=en` listing for `Unemployment Insurance Weekly Claims Report` rows, downloads each release's PDF (DOL serves the press-release URL as `application/pdf` directly when the request carries the right browser headers + cookie jar — Akamai's `sec_cpt` challenge), parses the headline figures from the bottom data table with a narrative fallback, and writes both Initial Claims (week ending five days before the Thursday release) + Continuing Claims (one week earlier) onto distinct `cal_econ_event` rows sharing the same release datetime. The week-ending date comes from the PDF narrative so federal-holiday-shifted Wednesday releases land the right reference Saturday; total-outage detection in the scheduler covers `releases_failed` against `listing_entries`. Both connectors join `ALL_CONNECTORS` + `ALL_VALUE_SIDE_CONNECTORS`; both write rows post-publication and fall through to the hourly baseline rather than carrying a burst predicate (matches the ECB pattern). `agency_registry.py` adds `EIA` + `DOL` for provider attribution; the parity whitelist stays empty until canonicalize aliases + magnitude-suffix alignment specs land in a follow-up. 28 mocked tests cover both connectors against captured fixture HTML + a real-captured DOL PDF; live HTTP stays out of CI.
- **P2b-schedule-drift (shipped — BEA schedule parser refitted to 2026 DOM):** `parse_schedule_html` rewritten against the live `id="release-schedule-table"` shape. Old-DOM assumptions (separate `Release Date` / `Release Time` / `Reference Period` columns, year-per-row) removed; new parser pulls the year from the table's `Year YYYY` header, reads the stacked `<div class="release-date">` + `<small class="text-muted">` out of the scheduled-date cell, reads the release name from the `release-title` cell, and pulls the reference period inline from the title tail after the first comma. `"Gross Domestic Product"` and `"GDP"` both land as Real GDP in the match fragments — BEA switched to the abbreviation in the 2026 refactor but the full form may return on a later rewrite. `_match_release` applies exclude / match fragments against the title's leading segment (everything before the first comma) so multi-indicator rows like `"GDP (Third Estimate), Industries, Corporate Profits, State GDP, and State Personal Income, 1st Quarter 2026"` still project as headline GDP — the regional / industry excludes veto only when they appear in the headline position itself. TBA rows (no `release-date` div) are silently skipped. Fixture at `tests/fixtures/bea_schedule/news_schedule.html` regenerated to mirror the live DOM. End-to-end live run (`schedule_bea_calendar`) against `bea.gov/news/schedule` on 2026-04-22 lands 18 rows (9 GDP across three stages × three quarters + 9 Personal Income across nine months), zero row_issues.
- **P6-live (shipped — first parity run, alias expansion, UTF-8 fix):** `statements.py` fixed a `requests` ISO-8859-1 decode bug corrupting U+2011 non-breaking hyphens in FOMC mixed-number rate strings (5 parse failures on 2022-06-15 / 2025-09-17 / 2025-12-10 / 2026-01-28 / 2026-03-18). Nine alias additions to `_official_shared/canonicalize.py` surfaced by the live run (TE labels "Non Farm Payrolls", "Fed Interest Rate Decision", "Marginal Lending Rate", and BLS long-form labels for AHE / AWH / ECI / Core PPI; one Codex P2 correction — `ecb interest rate decision` maps to `ECB_MP_DECISION`, not `ECB_MRO`). First parity report at `docs/validation/calendar_parity_2026-04-22.md`: FOMC 3/3=100%, NFP/AHE/AWH/UNEMP 5/7=71%, CPI/Core CPI 4/5=80%. Documented open gaps: ECB announcement vs effective-date split (P3a design decision, effective-date SDMX observations won't match TE's decision-date rows until the anchor is unified); pre-Nov 2025 approximate-precision rows (synthetic end-of-month timestamps on API-only rows before the scheduler ran).
- **P8 (shipped — TE recurring ingestion retired, official sources promoted to active):** `calendar_econ_backfill` / `calendar_econ_sync_updates` removed from any recurring schedule (no cron config exists in the repo — the operational step is to remove any external scheduler entry for these ops and downgrade the TE subscription). Cutover date 2026-04-22. Architecture doc updated accordingly.

Issue #31 — cron-deploy of the official-source recurring refresh:

- **P1 (shipped — systemd-user timers wrap the existing service ops):** `scripts/calendar_refresh_schedules.py` and `scripts/calendar_sweep_values.py` are thin entry-points that instantiate `LocalMacroDataService` and call `calendar_econ_refresh_schedules` / `calendar_econ_sweep_values` respectively, appending a structured JSON line per run to `.macro-data/logs/calendar_refresh_schedules.log` and `.macro-data/logs/calendar_sweep_values.log`. `scripts/calendar_refresh_schedules_wrapper.sh` and `scripts/calendar_sweep_values_wrapper.sh` add `flock --nonblock` guards. The two `systemd --user` units at `scripts/systemd/calendar-schedule-refresh.{service,timer}` and `scripts/systemd/calendar-value-sweep.{service,timer}` schedule the refresh at `*-*-* 04:00 UTC` (daily, ahead of the 06:00 UTC parity tripwire) and the value sweep at `*-*-* *:15` (hourly, staggered off the refresh slot so the two timers never collide on the engine DB). Both timers carry `Persistent=true` for catch-up on resume; both unit files set `Restart=no` because per-connector failure isolation is owned by the scheduler's `_run_connector_with_breaker` driver and `cooling_until_ms`, not by systemd retries. 7 mocked tests cover argument parsing, op-args forwarding, exit codes, and structured-log shape for both entry-points.

Issue #22 — daily TE parity tripwire:

- **P1–P5 (shipped — daily T+1 parity job):** `src/ingestion/calendar/te_api/daily.py` adds a single-day TE pull (`/calendar/country/All/{date}/{date}`) that writes through the existing parser + projector + vintage writer; idempotent via the same content-hash dedup the bulk backfill relies on. `src/ingestion/calendar/agency_registry.py` is the single source of truth on agency ownership — 24 agencies registered, with the indicator whitelist scoped to **value-comparable** `(country, canonical)` pairs only. Indicators where the connector stores a level / index but TE publishes a YoY rate (BLS CPI / NFP / Core CPI / PPI / AHE / JOLTS, BEA Personal Income / PCE / Corporate Profits, Census Housing Starts / Building Permits, NAR Existing Home Sales) are deliberately omitted; NBS is omitted entirely until value-side ingestion lands. Country codes follow the upstream connector outputs — euro-area is `EU` (matches the TE country map). `src/ingestion/calendar/parity_daily.py:compare_daily(connection, target_date)` is the pure comparator: for each TE actual on the target date, it finds the agency-side first-release vintage (earliest `observed_at` carrying a non-null `actual` from `calendar_event_vintages`) and enforces strict equality after Decimal normalisation (`%` / commas / whitespace stripped, optional per-canonical scale factor); ratios that hit a clean ×1000 / ×100 / ×0.01 factor are tagged as likely alignment bugs. `src/ingestion/calendar/parity_filer.py` wraps `gh issue list/create/comment/close`: one open issue per agency keyed by `parity-drift` + `agency:<id>`, anomalies append a comment, 7 consecutive clean days auto-close, re-anomaly after auto-close opens a fresh issue. **Operator-closed false alarms are honored** — when state remembers an open issue but GitHub no longer does and the prior clean streak is below the auto-close threshold, the closure is treated as an operator triage and the agency is suppressed until the next clean comparator run; without this guard the filer would re-open the same issue every morning. State lives at `.macro-data/parity_state.json`. `scripts/parity_daily.py` is the entry-point — pulls TE for `--date` (default yesterday UTC), refreshes the per-day backup snapshot via SQLite's online backup API into `.macro-data/backups/te_calendar_<date>/engine.db`, runs the comparator, files reports, appends a structured JSON line to `.macro-data/logs/parity_daily.log`. Two consecutive run failures auto-file an `agency:infra` issue. `scripts/parity_daily_wrapper.sh` adds a `flock --nonblock` guard for the `systemd --user` unit at `scripts/systemd/parity-daily.{service,timer}` — daily 06:00 UTC, `Persistent=true` so a missed firing on a suspended laptop catches up on resume. API budget: 1 call/day = ~31/month against the Basic-plan 1000/month cap. 21 new mocked tests cover daily pull (idempotency, revision append), comparator (clean / TE-only / mismatch / first-release vs revision / pct-suffix / unknown indicator), and filer (create / comment / 7-day close / auto-close-then-re-anomaly / operator-suppression / dry-run / infra failure).

Issue #13 — US coverage beyond BLS / BEA / Fed:

- **P1 (shipped — Census connector):** `src/ingestion/calendar/census_api/` adds the Census Bureau as provider id `census` (`cal_provider.precedence=100`). Value side uses the official EITS API (`api.census.gov/data/timeseries/eits`) and filters dataset/year payloads by stable Census tuples: Retail Sales MoM (`marts`, `MPCSM`, `44X72`, SA), Durable Goods Orders MoM (`advm3`, `MPCNO`, `DXT`, SA), Housing Starts (`resconst`, `TOTAL`, `ASTARTS`, SA), and Building Permits (`resconst`, `TOTAL`, `APERMITS`, SA). Schedule side scrapes the Census economic-indicator list-view calendar (`calendar-listview.html`) and emits one event per whitelisted indicator/reference period; the New Residential Construction row fans out into both Housing Starts and Building Permits. Schedule and value rows share the same `provider_event_id`, anchored on reference-period date, so `project_schedule_events` owns the scheduled ET release datetime and `project_events` fills `actual`. Service ops: `calendar_econ_fetch_census` and `calendar_econ_schedule_census`; recurring scheduler includes `census` in both schedule and value-side rosters. `scripts/validate_calendar_acquisition.py --provider census` dry-runs or probes the EITS coordinates. Focused fixtures/tests cover registry, parser, value filtering, schedule parsing, merge order, service dry-run, provider seeding, parity roster, and scheduler integration.

- **P2 (shipped — ISM connector):** `src/ingestion/calendar/ism_api/` adds the Institute for Supply Management as provider id `ism` (`provider_type=market_data`, `cal_provider.precedence=100`). Schedule side scrapes ISM's public Report Release Date Calendar and emits Manufacturing PMI releases at 10:00 America/New_York, anchored on the prior reference month because the May release publishes April data. Value side discovers the current Manufacturing PMI report URL from the ISM reports hub, parses the headline PMI and prior reading from the report HTML, and writes through `project_events`; schedule and value rows share `provider_event_id` on `(provider, country, canonical indicator, reference_date)`. Service ops: `calendar_econ_fetch_ism` and `calendar_econ_schedule_ism`; recurring scheduler includes `ism` in both schedule and value-side rosters. `scripts/validate_calendar_acquisition.py --provider ism` dry-runs or probes the calendar page plus current-report discovery. Focused fixtures/tests cover registry, schedule parsing, DST conversion, current-report discovery/value parsing, merge order, zero-entry schedule failure, service dry-run, provider seeding, parity roster, scheduler integration, and validator dry-run/fixture probes.

- **P3 (shipped — U Michigan connector):** `src/ingestion/calendar/umich_api/` adds University of Michigan Surveys of Consumers as provider id `umich` (`provider_type=market_data`, `cal_provider.precedence=100`). Schedule side discovers the current-year Release Dates document from the Survey Information page, converts the PDF to text with packaged `pypdf` plus `pdftotext` / `mutool` fallbacks, and emits Consumer Sentiment preliminary/final releases at 10:00 America/New_York. Preliminary and final rows share the reference month but anchor `provider_event_id` on `reference_date|release_stage`, so the two monthly releases stay distinct while the current-results value page can merge into the matching stage. Value side parses the public main-page results table (`Index of Consumer Sentiment`) and writes the current actual/previous values through `project_events`; schedule rows keep the exact release datetime. Service ops: `calendar_econ_fetch_umich` and `calendar_econ_schedule_umich`; recurring scheduler includes `umich` in both schedule and value-side rosters. `scripts/validate_calendar_acquisition.py --provider umich` dry-runs or probes the Survey Information release-date document plus current-results page. Focused fixtures/tests cover registry, schedule parsing, DST conversion, current-results value parsing, preliminary/final id separation, schedule/value merge order, zero-entry schedule failure, service dry-run, provider seeding, parity roster, scheduler integration, shared projector compatibility, and validator dry-run/fixture probes.

- **P4 (shipped — Conference Board connector):** `src/ingestion/calendar/conference_board_api/` adds The Conference Board as provider id `conference-board` (`provider_type=market_data`, `cal_provider.precedence=100`). Schedule side consumes the public JSON calendar endpoint (`/data/calendar/events.json.cfm`) and filters US rows for Consumer Confidence Index and Leading Index, preserving the published epoch-ms release datetime. Consumer Confidence anchors on the release month; US Leading Index anchors two months back, matching the current 2026 publication lag. Value side parses the public topic pages for current Consumer Confidence (`actual` index level, prior index level) and US Leading Index (`actual` monthly percent change, prior monthly percent change, raw index level in the payload). Service ops: `calendar_econ_fetch_conference_board` and `calendar_econ_schedule_conference_board`; recurring scheduler includes `conference-board` in both schedule and value-side rosters. `scripts/validate_calendar_acquisition.py --provider conference-board` dry-runs or probes the JSON calendar plus both current pages. Focused fixtures/tests cover registry, schedule parsing, DST conversion, value parsing, schedule/value id convergence, merge order, zero-entry schedule failure, service dry-run, provider seeding, parity roster, scheduler integration, shared projector compatibility, and validator dry-run/fixture probes.

- **P5 (shipped — NAR connector):** `src/ingestion/calendar/nar_api/` adds the National Association of Realtors as provider id `nar` (`provider_type=market_data`, `cal_provider.precedence=100`). Schedule side scrapes the public NAR Statistical News Release Schedule, filters Existing-Home Sales and Pending Home Sales Index rows, and stores 10:00 America/New_York release datetimes. Value side parses the public housing-statistics pages: Existing Home Sales stores the million-SAAR sales level, and Pending Home Sales MoM stores the signed month-over-month percentage. Schedule and value rows share `provider_event_id` on `(provider, country, canonical indicator, reference_date)`. Service ops: `calendar_econ_fetch_nar` and `calendar_econ_schedule_nar`; recurring scheduler includes `nar` in both schedule and value-side rosters. `scripts/validate_calendar_acquisition.py --provider nar` dry-runs or probes the schedule plus both current pages. Focused fixtures/tests cover registry, schedule parsing, DST conversion, current-value parsing, schedule/value id convergence, merge order, zero-entry schedule failure, service dry-run, provider seeding, parity roster, scheduler integration, shared projector compatibility, and validator dry-run/fixture probes.

- **Remaining:** Trade Balance via ITA (new BEA client path — ITA uses `Indicator` + `AreaOrCountry` params, not `TableName` + `LineNumber`; quarterly-only frequency mismatch with the monthly Census/BEA joint release means this needs a design decision before coding); P-sched live verification (BLS CPI + NFP landing within 30 min over three consecutive releases under cron — operator-driven).

Issue #14 — Japan coverage:

- **P1 (shipped — BoJ connector scaffold):** `src/ingestion/calendar/boj_api/` adds the Bank of Japan as provider id `boj` (`provider_type=central_bank`, `cal_provider.precedence=100`). Schedule side scrapes `boj.or.jp/en/mopo/mpmsche_minu/` — one HTML `<table>` per year under an `<h2 id="pYYYY">` heading; the first-column "Date of MPM" cell carries a comma-separated two-day pair (e.g. `"Jan. 22 (Thurs.), 23 (Fri.)"`, cross-month `"Apr. 30 (Wed.), May 1 (Thurs.)"`). The closing day is the second date; cross-month pairs resolve the closing day to the second month, and a Dec→Jan wrap bumps the closing year by one. `event_time_utc` uses a 12:00 Asia/Tokyo convention (JST has no DST, so 03:00 UTC year-round) — BoJ decisions land whenever the committee closes, typically between 11:30 and 12:30 JST; noon is the convention. Value side scrapes the statement page for each closing date (`boj.or.jp/en/mopo/mpmdeci/state_<YYYY>/k<YYMMDD>a.htm`) and extracts the policy-rate target from the stable sentence *"The Bank will encourage the uncollateralized overnight call rate to remain at around X.X percent"* — the sentence uses "remain" forward-looking even on a hike (the 2024-07-31 liftoff from ~0% to 0.25% phrased the new target the same way), so only the numeric rate is extracted; direction belongs to a downstream diff against the previous MPM. `actual` writes as a two-decimal string (`"0.50"` / `"0.25"`). `provider_event_id` anchors on the closing-date ISO string so the schedule and value rows share the same id and converge through the shared projector's merge CASE. Schedule-side write goes through `project_schedule_events` so a later re-scrape doesn't clobber the published `actual`; value-side uses `project_events`. Browser-UA header bundle borrowed from `fed_api.scraper` — `boj.or.jp` 403s on the default python-requests UA. Service ops: `calendar_econ_fetch_boj` and `calendar_econ_fetch_boj_values`; recurring scheduler includes `boj` (schedule side) and `boj-values` (value side) in the full rosters. Fixture HTML captured live 2026-04-24 in `tests/fixtures/boj_mpm_calendar/` (16 meetings across 2025 + 2026) and `tests/fixtures/boj_statements/` (four statements covering the hold / 2024-07-31 hike to 0.25% / 2025 holds at 0.5%); 24 mocked tests cover parse / cross-month / Dec-Jan wrap / statement extraction / projector merge / service ops — no real BoJ HTTP in CI.

- **P1a (shipped — BoJ Tankan connector scaffold):** `src/ingestion/calendar/boj_tankan_api/` ships the Large-Enterprises Business-Conditions Diffusion Index under the existing `boj` provider (no new `cal_provider` row). Two calendar indicators per release — `TANKAN_LARGE_MFG` (Tankan Large Manufacturers Index) and `TANKAN_LARGE_NONMFG` (Tankan Large Non-Manufacturers Index). Schedule side scrapes `boj.or.jp/en/statistics/tk/yoshi/index.htm`, a past-only table listing each quarterly release with its date and the URL of the result page (`/tk/yoshi/tkYYMM.htm` where YYMM encodes the survey reference quarter — `2603` = March 2026 survey, released on Apr 1 2026). `event_time_utc` uses **08:50 Asia/Tokyo** (the BoJ standard release clock documented in the annual `tkohyos.xlsx` schedule); JST has no DST, so the stored UTC stamp is always 23:50 of the prior date. `provider_event_id` anchors on `(indicator, reference_date)` so each release writes two distinct rows that the value-side upgrade upserts onto independently. Value side fetches each outline page and extracts the current quarter DI from the second row of the Business-Conditions > Large-Enterprises rowspan group (`row2[col=1]` under Manufacturing and Nonmanufacturing); `previous` comes from the same row's `col=0`, and `forecast` from the parenthesised "(N)" cell in row 1 (the prior survey's 3-month-ahead projection — BoJ's own forecast, not analyst consensus). Value-side auto-discovery passes the schedule-side `event_time_utc` through verbatim so the upsert preserves the canonical publish stamp (the outline page has no release-time block of its own). A 1-hour release-time buffer on auto-discovery parallels the BoJ MPM pattern and blocks circuit-breaker trips when the cron sweep fires on release day between yoshi-index refresh and outline page availability. Service ops: `calendar_econ_fetch_boj_tankan` and `calendar_econ_fetch_boj_tankan_values`; recurring scheduler adds `boj-tankan` (schedule side) and `boj-tankan-values` (value side). Fixture HTML captured live 2026-04-24 in `tests/fixtures/boj_tankan_schedule/` (12 quarterly releases covering 2023 Q2 → 2026 Q1) and `tests/fixtures/boj_tankan_outlines/` (three result pages: tk2603 / tk2512 / tk2503); 36 mocked tests cover parse / YYMM codec / indicator id distinctness / projector merge / 1h release buffer / service ops — no real BoJ HTTP in CI.

- **P2 (shipped — Statistics Bureau / e-Stat connector scaffold):** `src/ingestion/calendar/stat_bureau_api/` adds Japan's Statistics Bureau as provider id `stat-bureau-jp` (`provider_type=government_agency`, `cal_provider.precedence=100`). Two indicators ship: `Core CPI YoY` (`CORE_CPI`, percent change over the year for all items less fresh food) and `Unemployment Rate` (`UNEMPLOYMENT_RATE`, percent). Schedule side scrapes the official Statistics Bureau English release-schedule pages: CPI at `stat.go.jp/english/data/cpi/1582.htm` (Japan column) and Labour Force Survey at `stat.go.jp/english/data/roudou/1543.htm` (Basic tabulation column). Both use the 08:30 JST release convention, so the stored UTC stamp is 23:30 on the prior date. Value side calls e-Stat `getStatsData` with `ESTAT_APP_ID` in execute mode and the live-probed coordinates: CPI table `0003427113` with `cdTab=3`, `cdCat01=0161`, `cdArea=00000`; LFS table `0003005865` with `cdTab=02`, `cdCat01=000`, `cdCat02=08`, `cdCat03=0`, `cdArea=00000`. Monthly e-Stat time codes use `YYYY00MMMM` (`2026-03` -> `2026000303`). `provider_event_id` anchors on `(indicator, reference_date)` so schedule rows and e-Stat values converge. The recurring scheduler adds `stat-bureau-jp` (schedule side) and `stat-bureau-jp-values` (value side); service ops: `calendar_econ_fetch_stat_bureau` and `calendar_econ_fetch_stat_bureau_values`. Focused mocked tests cover CPI/LFS schedule parsing, JST conversion, e-Stat value parsing, provider seeding, scheduler/service registration, canonical aliases, and shared projector compatibility.

- **P4 (shipped — MoF Balance-of-Trade connector scaffold):** `src/ingestion/calendar/mof_api/` adds Japan's Ministry of Finance as provider id `mof-jp` (`provider_type=government_agency`, `cal_provider.precedence=100`). Single-indicator anchor — `BALANCE_OF_TRADE`, TE's highest-count Japan indicator (151 "high" importance events). Schedule side scrapes the Monthly Data column of `customs.go.jp/toukei/calendar/calend_e.htm`; the release time is 08:50 JST (documented in the calendar table header). Forward visibility is reliable — the calendar page carries the current plus next calendar year's dates well in advance, so no in-sweep re-seed is needed (unlike Tankan P1a). Aggregation rows (Fiscal Year / Calendar Year) are filtered out because they duplicate the corresponding monthly row's release day. The scraper resolves the Dec→Jan cross-year rule: a release month numerically less than the reference month (Dec 2025 data released on Jan 22) bumps the release year by one, while explicit year suffixes (`"Feb.17,2027"`) override. Value side fetches each release's structured XML feed at `customs.go.jp/toukei/shinbun/trade-st_e/<YYYY>/<YYYYMM>4e.xml` and extracts the headline `<sashihiki><sogakutonen>` (balance, millions of yen, first `<sogakutsuki name="pg1">` element). Deficits render with the Japanese `△` sign, which the parser normalises to a plain negative integer. XML parse is stdlib `xml.etree.ElementTree`; a reference-month cross-check against `<taishoymtonen>` fails loud if a caller-supplied reference disagrees with the feed. `actual` writes as a signed integer string (`"666977"` / `"-637610"`) in units of JPY million; `currency="JPY"` on both sides. `provider_event_id` anchors on `(indicator, reference_date)`; both sides use the per-release XML URL as `source_url` so a schedule re-seed never flips historical provenance. A 1-hour release-time buffer on value-side auto-discovery mirrors the Tankan / BoJ MPM pattern. Service ops: `calendar_econ_fetch_mof` and `calendar_econ_fetch_mof_values`; recurring scheduler adds `mof-jp` (schedule) and `mof-jp-values` (value). Fixture HTML + XML captured 2026-04-24 in `tests/fixtures/mof_calendar/` (16 monthly rows, Dec 2025 → Mar 2027) and `tests/fixtures/mof_trade_reports/` (four reports including two surpluses and two triangle-signed deficits); 39 mocked tests cover parse / year-wrap rule / aggregation-row filtering / deficit sign / reference-mismatch detection / projector merge / 1h release buffer / manual-replay event-time preservation / service ops — no real MoF HTTP in CI.

- **P3 (shipped — CAO Consumer Confidence connector scaffold):** `src/ingestion/calendar/cao_api/` adds the Cabinet Office / Economic and Social Research Institute as provider id `cao` (`provider_type=government_agency`, `cal_provider.precedence=100`). Single-indicator anchor — `Consumer Confidence` (canonicalizes to the shared `CB_CONSUMER_CONFIDENCE` token alongside the US Conference Board row; country disambiguation lives in `provider_event_id`, same pattern as MoF's reuse of BEA's `TRADE_BALANCE`). 142 "high" importance events in TE historical — the highest-count Japan indicator after BoT. Schedule side scrapes the five-column ESRI release-schedule table at `esri.cao.go.jp/en/stat/stat-schedule-e.html`, pulling column 4 ("Consumer Confidence Survey") forward. Release time is 14:00 JST documented in the Japanese twin (`stat-schedule.html`) footnote — JST has no DST, so the stored UTC stamp is always 05:00 of the same date. The scraper resolves the implicit-year rule (only the first tbody row carries an explicit year; subsequent rows drop it) and the January release → December reference wrap (`Jan.15,2027 / (Dec.)` → reference year 2026). A reference-release gap check guards against column drift (the scraper aborts loud if a gap > 1 month shows up). Value side fetches the `esri.cao.go.jp/en/stat/shouhi/shouhi-e.html` landing page once per sweep — CAO overwrites the page on each new release, so there is no per-release archive URL. Two deterministic sentences carry the headline: *"The Survey of March 2026 was released on April 9th, 2026"* and *"The Consumer Confidence Index (seasonally adjusted series) in March 2026 was 33.3, down 6.4 points from the previous month."* The parser cross-checks reference months between the two sentences (guarding against mid-edit / cache-stale pages) and extracts the SA series decimal as `actual` (formatted with `%g` so `33.3` renders cleanly and a rare integer result doesn't stamp as `33.000`). `currency=""` (diffusion-style index, not JPY-denominated — matches Tankan / ISM / U Michigan). `provider_event_id` anchors on `(indicator, reference_date)` so schedule and value sides converge. Both sides use the Consumer Confidence landing-page URL as `source_url`, so a schedule re-seed never flips historical provenance. The value-side sweep reads any stored schedule-side `event_time_utc` and passes it through to the upsert verbatim, keeping the canonical publish clock intact; when no schedule row exists yet (cron lag), `project_events`' full upsert creates it. Service ops: `calendar_econ_fetch_cao` and `calendar_econ_fetch_cao_values`; recurring scheduler adds `cao` (schedule) and `cao-values` (value). Fixture HTML captured 2026-04-24 in `tests/fixtures/cao_schedule/` (4 forward-looking ConsCon rows, April–July 2026) and `tests/fixtures/cao_consumer_confidence/` (two landing-page snapshots covering February + March 2026 releases); 41 mocked tests cover parse / implicit-year carry / Dec-Jan reference wrap / quarter-range guard / duplicate-reference guard / reference-release gap sanity / ordinal-suffix variants / reference-month mismatch detection / projector merge / event-time preservation / fetch + parse failure collection / canonicalize aliases / service ops — no real CAO HTTP in CI.

- **P3a (shipped — CAO GDP connector scaffold):** `src/ingestion/calendar/cao_gdp_api/` adds Japan's quarterly GDP Growth Rate QoQ under the existing `cao` provider. Schedule side scrapes the ESRI SNA archive index (`toukei_top.html`) and the latest yearly archive pages (`sna/data/sokuhou/files/<YYYY>/toukei_<YYYY>.html`), which list past first and second preliminary releases. `provider_event_id` anchors on `reference_date|release_stage` so the first preliminary and second preliminary rows for the same quarter stay distinct, matching the parity harness' staged-GDP handling. `event_time_utc` uses 08:50 JST, the release clock used by ESRI's GDP publication schedule; quarterly `reference_date` stores the quarter-end date and `reference_label` stores `Qn YYYY`. Value side opens each release menu (`qeYYQ[/_2]/gdemenuea.html`), resolves the `ritu-jk*.csv` link for "Real, Seasonally Adjusted Series (Quarter-to-Quarter)", decodes the CSV as CP932, and extracts the first data column (`GDP(Expenditure Approach)`) for the target quarter. `actual` stores the published percent-change string (for example `-0.6`), `unit="percent"`, and `currency=""`. Both schedule and value rows keep the report-menu URL as `source_url`; the raw value payload also carries the resolved CSV URL. The recurring scheduler adds `cao-gdp` (schedule side) and `cao-gdp-values` (value side); the value wrapper seeds the past-only archive before discovering pending rows, paralleling the Tankan pattern. Service ops: `calendar_econ_fetch_cao_gdp` and `calendar_econ_fetch_cao_gdp_values`. Focused mocked tests cover archive-year discovery, staged release parsing, stage-qualified ids, report-menu CSV resolution, CP932-style CSV parsing, projector merge, fetch/parse failure collection, scheduler registration, and service dry-runs.
- **P5 (shipped — METI connector scaffold):** `src/ingestion/calendar/meti_api/` adds the Ministry of Economy, Trade and Industry as provider id `meti` (`provider_type=government_agency`, `cal_provider.precedence=100`). Two indicators ship: `Industrial Production MoM Prel` (`INDUSTRIAL_PRODUCTION`, percent change from previous month) and `Retail Sales YoY` (`RETAIL_SALES`, percent change from the same month one year earlier). IIP schedule side reads METI's IIP release-calendar XML (`english/statistics/tyo/iip/yotei/xml/e-stat_iip_e.xml`) and filters preliminary rows; value side fetches each 2020-base preliminary report page (`b2020_<YYYYMM>se.html`) and extracts the Production row's seasonally-adjusted month-over-month change. Retail schedule side reads the Current Survey of Commerce landing page (`english/statistics/tyo/syoudou/index.html`) and parses the next preliminary release sentence; value side reads the current page's Outline PDF link, extracts text with `pypdf`, and parses the headline retail-sales table row. `event_time_utc` uses 08:50 JST for both surfaces, with a 1-hour value-side buffer in auto-discovery. Retail current-page value ingestion also creates the current release row when the forward schedule row is absent, so a release-day scheduler lag can still land the official value. The recurring scheduler adds `meti` (schedule side) and `meti-values` (value side); service ops: `calendar_econ_fetch_meti` and `calendar_econ_fetch_meti_values`. Focused mocked tests cover XML schedule parsing, retail schedule parsing, IIP report parsing, retail outline parsing, projector merge, scheduler/service registration, canonical aliases, and provider seeding.

- **Remaining phase:** none for the original P1-P5 Japan minimum slice.

Issue #21 — point-in-time vintage layer for calendar events:

- **P1 (shipped — schema + writer):** `calendar_event_vintages` table — `(id, event_id, provider, vintage_date, observed_at, actual, forecast, previous, metadata_json, scraped_at)` with `UNIQUE(event_id, provider, vintage_date)` and a `(event_id, provider, observed_at)` lookup index. Module-level `append_calendar_event_vintage_if_changed_with_conn(connection, ...)` performs a predecessor-aware compare (the row at-or-before the candidate's `observed_at`) so out-of-order writes still capture genuine intermediate states; class method `SQLiteEngineStore.append_calendar_event_vintage_if_changed(...)` wraps it. `CalendarEventVintageRecord` re-exported from `storage/__init__.py`.
- **P1.5 (shipped — TE backup seed):** `scripts/seed_te_calendar_vintages.py` reads `.macro-data/backups/te_calendar_2026-04-23/te_calendar.db` (264,792 rows, zero API calls) and projects up to two vintages per event: a current vintage at `max(last_update_epoch_ms, event_time_utc)` (clamp to release-time floor protects ~39K rows whose recorded LastUpdate predates release), and a first-print vintage at `event_time_utc` when the *next* same-ticker event's `previous` field differs from this event's `actual`. The next-period `previous` is the only authentic first-print source in the snapshot — TE's `revised` field stores the *previous-period* revised value, not this event's pre-revision state. Realised seed: ~277K vintages across 217,991 events; 59,110 events have 2-vintage chains. NFP coverage 95.5%, Retail Sales MoM 82.1%, ISM PMI 9.7%, headline CPI 0–9% (TE doesn't store CPI revisions in the same field — accepted data limit). Idempotent via the UNIQUE constraint.
- **P2 (shipped — connector hookup):** `_official_shared/projector.py:project_events` and `te_api/projector.py:project_events` call the writer helper for every record, plus the legacy `upsert_calendar_event` in `src/storage/sqlite.py` (covers forexfactory / investing / old TE scraper). Vintage write runs unconditionally — even when `cal_econ_event`'s observed_at freshness gate suppresses an UPDATE, the vintage row still records the change. Pre-release rows (forecast/previous populated, `actual` empty) keep their fetch-time `observed_at` so PIT queries between fetch and release see the consensus; the release-time clamp activates only when an `actual` is present. `vintage_date = observed_at` (not LastUpdate) so a row that reuses upstream LastUpdate across distinct revisions still appends instead of being swallowed by `UNIQUE`. Original LastUpdate is preserved in `metadata_json` under `te_last_update` / `source_last_update`.
- **P3 (shipped — PIT read API):** `calendar_actual_as_of(event_id, provider, t) -> CalendarEventVintageRecord | None` — canonical PIT lookup; tie-breaks to the higher `id` on identical `observed_at`. `calendar_vintage_history(event_id, provider) -> list[...]` — full chain ascending. SQL comparisons go through `julianday(observed_at) <= julianday(?)` rather than TEXT `<=`, because lexicographic compare on ISO timestamps with fractional seconds (`...:00.500Z`) would falsely treat them as `<=` a same-second whole-second cutoff (`'.'` < `'Z'`) and leak future state. 15-test unit suite under `tests/test_calendar_event_vintages.py` covers append-on-change, no-future-leak (incl. fractional-second case), tie-breaking on identical `observed_at`, multi-step revision chains, and provider isolation.
- **Out of scope:** TE `/historical/ticker` enrichment; multi-revision chains beyond the backup's 2-vintage reconstruction; vintage-vs-vintage parity tooling; downstream HTTP API exposure of PIT (internal first).

Issue #36 — Wayback evidence-archive URL per vintage row:

- **P1 (shipped — sweep-tail submitter):** `calendar_event_vintages` extended with `source_url TEXT NOT NULL DEFAULT ''` (snapshotted at insert so revision-time URL changes keep their per-vintage anchor), `evidence_archive_url TEXT` (Wayback snapshot URL or NULL), and `evidence_last_attempt_at TEXT` (stamped on every submission attempt so failed rows rotate to the back of the retry queue and a block of unarchivable URLs cannot stall the head). Partial index `idx_cal_evidence_pending(observed_at) WHERE evidence_archive_url IS NULL` drives the retry scan. New module `src/ingestion/calendar/evidence_archive.py` holds `submit_save_request(source_url) -> str | None` (Save Page Now call; reads Content-Location, falls back to final redirect URL; respects optional `WAYBACK_S3_ACCESSKEY`/`WAYBACK_S3_SECRET` env auth) and `archive_pending(connection, *, limit, submitter) -> {scanned, archived, failed}` (scans NULL rows with non-empty `source_url`, never-tried first then oldest-tried, up to limit; runs all network submissions before any UPDATE so SQLite's writer lock isn't held across HTTP). Default `limit=12` keeps the anonymous-quota deployment under SPN's ~15 saves/min ceiling; deployments with S3 auth can pass a higher limit. The tail runs at the end of each `_op_calendar_econ_sweep_values` execute-mode invocation (skipped on `dry_run=True`); response envelope carries an `evidence_archive` counters dict. Set `MACRO_DATA_WAYBACK_DISABLED=1` (literal `"1"` only — `"0"` does NOT disable) to make the tail a no-op. Two vintage rows on the same `(provider, provider_event_id)` carrying identical `source_url` produce two independent SPN submissions — one PIT observation, one snapshot timestamp. Trade-off vs the issue body's "fire-and-forget at insert + retry tail" wording: sweep-tail-only collapses both paths into one — every new row is NULL on insert and gets archived on the next hourly cycle. Up to ~1-hour archival latency; `observed_at` remains authoritative for PIT ordering. Avoids the commit-race hazard of dispatching async submission before the parent transaction commits.
- **Out of scope:** local WARC/PDF redundancy; historical TE backfill; whitelist filtering; Wayback CDX/playback proxy; insert-time async dispatch (deferred until measured 1-hour latency proves insufficient).

### 6. Release scheduling + availability

- `release_schedule` (rules) + `release_status` (per-release tracking). Date-math resolvers in `ingestion/release_schedule.py` compute `next_expected` per concept.
- State machine: `PENDING → WAITING → FETCHED → CONFIRMED / STALE / FAILED`. Retry ladder 1m / 5m / 15m / 1h / 4h.
- Three alert types: `DELAY` (missed expected release by 30m), `FAILED` (retries exhausted), `MISMATCH` (cross-source divergence over threshold).

### 7. Source capabilities + catalog sync

- `source_capability` registers each source's discovery / latest-sync / status contract. `catalog_entity` + `catalog_sync_*` persist crawled catalogs and run logs — used for OECD / World Bank / ILO / SDMX catalogs.

### 8. RAG sidecar

- `src/rag/`: chunker, BM25 lexical, embeddings, Milvus vector store, reranker, retriever, orchestrator, policy layer. Reads from `document_blob`; writes to a sibling index. Optional at runtime.

### 9. Trading / research artifacts (scaffolded)

- `trade_signals`, `decision_log`, `position_state`, `performance_records`, `analytical_observations`, `research_artifacts`, `generated_notes`, `regime_snapshots`, `portfolio_*`, `group_*`, `client_profiles`, `conversation_threads` / `_messages`, `delivery_queue`, `subagent_runs`. Tables exist; most are scaffolding pending the downstream services that will populate them.

---

## Service boundary

**HTTP API is the only downstream contract.** Every feature this repo ships
must be reachable through `macro-data-api` routes. Downstream services (analyst
UI, RAG consumers, agents, notebooks, scheduled jobs, external teams) are not
permitted to:

- import from `src/` as a Python library,
- read `engine.db` directly,
- shell out to the `macro-data-service` CLI,
- tail files, parse logs, or scrape any internal artifact.

If a consumer needs a capability, the path is always: add a service op →
expose an HTTP route → call it. No exceptions, no "for now we'll just
import it." Breaking this rule couples downstream to our storage shape and
defeats the whole aggregation-layer premise.

- **HTTP API** (`macro-data-api`) — the contract. Routes are a thin mapping
  over `LocalMacroDataService` ops; schema is the `contracts.py` DTOs.
- **CLI** (`macro-data-service`) — operational only. Subcommands like
  `refresh`, `refresh-source`, `schedule --run`, `health`, plus the new
  `list_items` op (issue #5). Used for bootstrap, backfills, and debugging
  by repo maintainers. Not part of the downstream contract.
- **Source family discriminator** (issue #5, shipped): every
  `IngestionSourceDefinition` carries a `family` tag; `list_sources` returns
  `[{name, family}, ...]`. `list_items` unions documents + indicators +
  market bars under a single subject, with a `family` filter — exposed over
  HTTP, not only via CLI.

---

## Where things live — quick map

```
src/
  contracts.py              Core DTOs (Event, CalendarItem, MarketSnapshot, RegimeState, ...)
  storage/
    sqlite.py               SQLiteEngineStore — composes 8 query mixins + connection-management base
    schema.py               apply_schema(connection) — every CREATE TABLE / INDEX / additive ALTER
    models/                 40 frozen record dataclasses, per-domain (issue #58 Tier 2.1A)
    queries/                Per-domain SQL helpers, mixed into SQLiteEngineStore (issue #71 Tier 2.1B)
      analytical.py         regime_snapshots, generated_notes, analytical_observations, …
      calendar.py           calendar_events, vintages, indicator/alias, release_schedule/status + free helpers
      documents.py          doc_source/release_family, document/blob/extra, FTS, item_subjects, list_items_*
      indicator.py          indicators, vintages, obs_*, concept_map, source_capability + catalog_*, seed dicts
      market.py             market_prices/instruments/symbol_history/price_bars + portfolio_*
      messaging.py          client_profiles, conversations, delivery_queue, group_*
      news.py               news_articles, trend_topics, article_fingerprint, news_context scoring
      trading.py            trade_signals, decision_log, position_state, performance, trading_artifacts
    STORAGE.md              Table-by-table narrative (kept in sync with schema.py)
    subjects.py             Subject vocabulary loader + tagger
    subjects.yaml           Canonical subject list (edit here, sync via subjects.py)
  ingestion/
    sources.py              IngestionOrchestrator — scheduling, retry, health, source family
    source_capabilities.py  Capability registry + discovery/latest-sync adapters
    release_schedule.py     Date-math resolvers + availability state machine
    validation/             Data quality + cross-source checks
    sdmx/                   Unified SDMX engine (base client, parsing, providers/)
    timeseries/, news/, documents/, trends/, market/, calendar/  One dir per domain
    _shared/                http_transport, url_canon, selector versioning
  macro_data/
    service/                LocalMacroDataService — the one interface downstream reads
      base.py               __init__, invoke router, _ensure_* one-shot seeders
      _calendar.py          calendar econ + corp ops + event_to_dict
      _timeseries.py        indicator/catalog/concept/release/surprise/fed-comm ops
      _documents.py         document feed + cross-type list_items
      _news.py              news + trends + article fetcher
      _market.py            market snapshot + live-markets fetcher
      _ops_health.py        refresh-all/run-schedule/source listing/health/alerts
    cli.py, server.py       Thin boundaries
    factory.py, client.py   Wiring + programmatic access
  rag/                      Local RAG (see src/rag/README.md)
```

---

## In flight right now

| Issue | Branch | Slice |
|---|---|---|
| #8 | `fix/issue-8-retire-legacy-calendar` | Final retirement slice: legacy HTML calendar source is inert, old read helpers point at `cal_econ_event`, and `/v1/calendar` is the downstream contract. |

Closed recently (context only — the code is the source of truth):

- #5 — source family tag + cross-type `list_items`.
- #1 — market-data layer with OpenFIGI/ISIN identity + lazy repair.
- #2 — unified subject vocabulary.

---

## Invariants worth knowing

1. **Downstream consumes only the HTTP API.** No library imports, no direct
   `engine.db` reads, no CLI shelling, no file/log tailing. Every feature ships
   with a route or it doesn't ship. (See "Service boundary" above for the full
   rule.)
2. **Revisions are never lost.** `indicator_vintages`, `cal_econ_raw` /
   `cal_corp_raw` append; PIT projections are derived views over them.
3. **The service layer exposes one contract.** Storage lanes can split (macro
   vs calendar vs corporate) but resolution presents a single shape —
   `resolve_indicator`, `get_market_history`, `list_items`, and (coming)
   `v_calendar_item` reads, all via HTTP.
4. **Upstream IDs are kept.** `provider_event_id`, `provider_series_id`,
   `canonical_url` — we never discard the upstream handle, even when we
   synthesize our own.
5. **Concept map is the cross-source bridge.** Source-native IDs → canonical
   concept IDs (CPI_US, UNEMP_US, …) → `resolve_indicator` ranks and returns
   with provenance.
6. **`subjects.yaml` is authoritative for the subject vocabulary.**
   `subjects` / `subject_aliases` / `item_subjects` tables are derived.
